# Feuille de route Atelier

Document vivant. Le cadrage (`decisions.md`) dit pourquoi ; ce fichier dit quoi et dans quel ordre. Il est mis à jour à la fin de chaque session de travail.

Règle d'entrée (D59) : rien n'entre dans une version tant que le site en cours de construction n'en a pas besoin. Ce qui n'est pas nécessaire au site de recette va dans la version suivante.

États : `[x]` fait · `[~]` en cours · `[ ]` à faire · `[?]` à trancher

---

## v0 · Fondations — site de recette : vitrine de photographe

Critère de sortie : le site de la photographe est en ligne sur un sous-domaine, construit entièrement dans Atelier, et utilisé.

### M0 · Socle — fait le 6 septembre 2026

- [x] Spécification du modèle de document (`document-model.md`)
- [x] Paquet modèle : types, schéma de validation, opérations inversibles, arbre, historique
- [x] Moteur de rendu : CSS (jetons, modes, styles partagés, points de rupture, états) et rendu React de tous les types de nœuds
- [x] Site d'exemple (Marie Lambert) : thème, composants, base « Projets », 5 pages
- [x] Coquille d'éditeur : pages, calques, aperçu réel en iframe, largeurs, mode sombre, sélection croisée
- [x] Persistance : dépôt fichier et Supabase, journal d'opérations versionné, conflit 409, instantanés et compactage
- [x] Annuler et rétablir, regroupement des frappes, aperçu vivant sans rechargement
- [x] Inspecteur provisoire : nom, texte simple, styles de base, monter, descendre, dupliquer, supprimer

### M1 · Système de design de l'éditeur — fait le 6 septembre 2026 (base, à faire évoluer)

Décision du 6 septembre : interface dense et sombre, résolument outil pro, mais qui ne fait pas peur au débutant (libellés lisibles, infobulles, pas d'icônes seules, contrastes soignés). Le système est posé avant les panneaux pour que chaque panneau naisse dedans ; il évoluera.

- [x] **Jetons de l'éditeur** : fonds, surfaces, lignes, textes, accent, états (sélection, survol, danger), rayons, espacements, typographie utilitaire (IBM Plex Sans / Mono, cohérent avec le cadrage)
- [x] **Primitives d'interface** (`apps/editor/src/ui`) : bouton, bouton icône, champ, champ numérique avec unité, sélecteur, onglets, panneau et section repliable, ligne d'arbre, barre d'outils, badge d'état, raccourci clavier, infobulle
- [x] **Coquille refaite** avec ces primitives : barre supérieure (site, page, modes, largeurs, annuler/rétablir, état, aperçu, publier), panneau gauche à onglets (Pages, Calques, Ajouter), zone de canvas, panneau droit
- [x] Inspecteur provisoire rhabillé avec les primitives (remplacé en M3)
- [x] Icônes : Lucide

### M2 · Édition structurelle — fait le 6 septembre 2026

Ce qui empêche aujourd'hui de construire une page : on ne peut ni ajouter ni déplacer.

- [x] **Ajouter un bloc** : onglet Ajouter (boîte, texte, image, lien/bouton, liste, séparateur, colonnes, section, vue de base, formulaire, composant existant), insertion avant/après/dedans la sélection
- [x] **Déplacer depuis les calques** : glisser-déposer dans l'arbre avec indicateur avant/après/dedans, respect des types conteneurs
- [x] **Déplacer depuis le canvas** : glisser un élément sélectionné dans l'aperçu, cible calculée depuis le DOM réel, aperçu de la position
- [x] **Largeur d'aperçu libre** : champ en pixels (320 à 4000, validé à la fin de la saisie), poignée de redimensionnement, point de rupture actif, mise à l'échelle automatique quand le cadre dépasse la zone visible (badge de zoom)
- [x] Sélection au clavier : flèches pour parent/enfant/frères, Suppr, ⌘D dupliquer, Échap
- [x] Renommer un calque en double-cliquant (ou Entrée)
- [x] Arbre : replier/déplier mémorisé, ancêtres ouverts et défilement à la sélection
- [x] Pages : créer (en-tête et pied de page du site ajoutés, adresse déduite du nom), dupliquer, réglages (nom, adresse, titre et description SEO), supprimer (sauf la dernière et les modèles de base) — ajouté le 7 sept. au premier retour de l'audit

### M3 · Panneaux Design (D19) — fait le 7 septembre 2026 (reste l'audit d'usage)

Le panneau de droite actuel est un échafaudage. Il est remplacé, pas amélioré.

- [x] **Disposition** : affichage, direction, retour à la ligne, alignement, répartition, écart, grille (colonnes, lignes, nombre de colonnes égales), position et décalages, calque, débordement, réglages « dans le parent » (grandir, rétrécir, base, aligner, ordre, colonne/ligne de grille)
- [x] **Espacement** : schéma de la boîte (marge, remplissage), une case par côté, côtés liables, couleur du texte selon la source
- [x] **Dimensions** : largeur, hauteur, min et max, ratio, unités et jetons
- [x] **Typographie** : police (thème, jetons, système), taille, interligne, graisse, espacement, couleur, alignement, italique et décorations, casse, retour à la ligne
- [x] **Apparence** : fond couleur, dégradé (type, angle, arrêts) ou image (ressource, taille, position), bordure (épaisseur, style, couleur), arrondi global ou par coin, ombre (jeton ou libre), opacité
- [x] **Effets** : rotation, échelle, décalage (ou texte brut si transformation complexe), transition (propriété, durée, courbe), filtre, curseur
- [x] Contrôles communs : champ de longueur avec unité, jeton (◇), mots-clés, flèches et Alt+glisser · pastille de source et réinitialisation · groupe de boutons exclusifs · sélecteur de couleur (pastille native, texte libre, jetons avec valeur par mode)
- [x] **États** : sélecteur Survol / Actif / Focus dans l'inspecteur, réglages posés sur l'état au point actif, valeurs de l'état normal marquées héritées, état forcé dans l'aperçu (attribut `data-force-state` émis avec chaque pseudo-classe)
- [x] **Points de rupture** : édition sur le point actif (celui de la largeur d'aperçu), indicateur de source par contrôle (bleu : posé ici · ambre : hérité d'un point plus large · violet : style partagé · gris : défaut du thème), réinitialisation d'une surcharge, section « Responsive » listant les surcharges de l'élément par point
- [x] Réglages des points de rupture du site : ajouter, renommer, changer un seuil, supprimer si aucun réglage ne l'utilise ; grands écrans en largeur minimale en v1
- [x] **Styles partagés** : appliquer, créer depuis la sélection, modifier, détacher, voir les usages (fait le 7 sept., session 3 ; case oubliée, corrigée lors de la revue du 8 sept.)
- [x] **Thème** : onglet Thème avec les jetons par groupe (couleurs par mode, polices, tailles, interlignes, espacements, arrondis, ombres, largeurs), ajout et suppression
- [x] Propriétés par type : image (ressource du site ou ajout par adresse, alt, ajustement, ratio, priorité), lien ou bouton (page, adresse, email, téléphone, ancre, nouvel onglet), texte et boîte (balise sémantique, D38), collection (base, tri, limite)
- [x] Palette de commandes ⌘K : annuler, rétablir, modes, largeurs, panneaux, pages, ajout de blocs, sélection d'un calque par son nom ; raccourcis affichés dans la palette
- [x] **Audit d'usage n°1** (7 sept. 2026) — retours d'Antoine et suites :
  - [x] Modifier le texte directement dans le canvas → double-clic sur un texte, Entrée valide, Échap annule (texte simple ; le texte riche viendra avec M4)
  - [x] Glisser un bloc depuis Ajouter vers le canvas ou les calques
  - [x] Dimensions incompréhensible et qui déborde → panneau sur une colonne, explication, libellés clairs
  - [x] ⌘C, ⌘X, ⌘V sur les calques
  - [ ] Sélection multiple (Maj / Alt) → M4, chantier à part : sélection, déplacement et style groupés
  - [x] Changer la police ou l'alignement ne faisait rien → bug de priorité CSS des défauts du thème (corrigé avec `:where()`, et ça explique aussi le titre qui ne se centrait pas)
  - [x] Faire varier un nombre en glissant → glisser sur le libellé de la propriété (Maj ×10)
  - [x] « Bureau » restait à la largeur du panneau (868 px = Tablette) → force 1280 px, réduit à l'échelle si besoin
  - [x] Panneau de droite peu clair → explication sous chaque section, sections renommées et réordonnées (Élément, Texte/Image/Lien, Disposition, Espacement, Dimensions, Typographie, Apparence, Effets, Styles partagés, Responsive, Avancé)
  - [x] « Jeton » incompréhensible → « valeur du thème » partout, explication en tête de l'onglet Thème
  - [x] États : savoir qui a un survol et sur quoi → badge avec le nombre de réglages par état, liste des propriétés modifiées par l'état actif, « Tout retirer »
- [x] **Audit d'usage n°2** (7 sept. 2026, 30 points) — traités :
  - [x] « Les px ne sont pas des px » → une valeur du thème dans le schéma de boîte s'affichait par son nom (12 = space.12 = 96 px) ; affiche désormais la vraie valeur, soulignée en pointillés
  - [x] Infobulles du (i) → `title` natif
  - [x] Polices : ajout de n'importe quelle police Google par son nom et ses graisses (onglet Thème → Polices), chargée à chaud dans l'aperçu ; l'import d'un fichier de police attend M5
  - [x] Suppr / Backspace et tous les raccourcis fonctionnent aussi quand l'aperçu a le focus (touches transmises)
  - [x] En-tête de l'inspecteur (actions, états) collant au défilement
  - [x] Vertical / horizontal « inversés » → les icônes suivent maintenant l'axe réel selon le sens (ligne ou colonne), libellés en langage courant (à gauche, centré, en haut…)
  - [x] Modifier une dimension à la souris → glisser sur le libellé (déjà) ; le chip d'une valeur du thème s'édite d'un clic sans passer par la croix
  - [x] Auto ≠ taille minimale → Dimensions avec quatre modes : Auto, Ajustée au contenu (fit-content), Remplit le parent (100 %), Fixe ; explication ; retour à auto par la pastille
  - [x] « Dans le parent » louche → « Place dans son parent », réglages en langage courant (prend l'espace libre, peut rétrécir, alignement)
  - [x] Cliquer un réglage d'état mène au réglage (section ouverte, défilement, surlignage) ; idem depuis Responsive
  - [x] Filtres → Flou, Luminosité, Contraste, Saturation, Noir et blanc (texte brut seulement si un filtre inconnu existe)
  - [x] Curseur dans l'aperçu : flèche partout, texte seulement en édition, main fermée en déplacement
  - [x] Bandeau « Réglages posés sur… » qui débordait → reformulé et sur deux lignes
  - [x] Voir ce qui est surchargé par taille d'écran → Responsive déplie chaque taille avec ses réglages et leurs valeurs
  - [x] Tailles à la Figma (fill / hug) → oui, c'est le mode Ajustée / Remplit ci-dessus
  - [x] Grille de mise en page fluide → décidée le 7 sept. (avant Écriture) et faite : réglage dans Thème (colonnes, gouttière, marge, largeur max., colonnes et marge par taille d'écran), calque affichable par-dessus l'aperçu (⌃G, bouton dans la barre, palette), « Calquer sur la grille du site » pour une disposition en grille, fractions ¼ ⅓ ½ ⅔ ¾ 1 dans Dimensions, gouttière reprise par le bloc Colonnes. L'accrochage automatique lors d'un glissement n'est pas fait (le modèle est en flux CSS, pas en positions libres)
  - [x] Débordement du panneau (« Ligne de base ») → icône, panneaux sans débordement horizontal
  - [x] « Avancé » bizarre → renommé « CSS brut », placé en dernier, présenté comme réservé aux développeurs ; l'identifiant y est déplacé
  - [x] Animer un changement d'état → bouton « Animer les changements » dans la ligne d'état (transition 200 ms), lien vers la transition
  - [x] Calques d'un composant (pied de page) → bouton « Ouvrir » sur la ligne du composant ou « Modifier le composant » dans l'inspecteur ; bandeau et retour à la page
  - [x] Console d'erreurs Next → badge masqué (`devIndicators: false`)
  - [x] Gérer un lien → section Lien de l'inspecteur (déjà), expliquée ; un lien dans un texte attend le texte riche de M4
  - [x] Balise / Identifiant → « Balise HTML » expliquée, identifiant déplacé dans CSS brut
  - [x] Ombres sm / md → libellés « petite (sm) », « moyenne (md) »… pour les valeurs du thème nommées par taille
  - [x] Bouton avec survol sans réglage → les états venant d'un style partagé sont listés avec ◆ et le nom du style
  - [ ] Refonte complète du panneau de droite → faite par touches ci-dessus ; une passe globale d'harmonisation est prévue après l'audit n°3
  - [x] Suite du 7 sept. (soir) : infobulles réelles (composant Tooltip, immédiates) sur les (i), les boutons icône, les pastilles, les options et les libellés ; panneau droit à 340 px et libellés courts ; espace perdu autour du canvas supprimé ; deux sections « Polices » du thème distinguées (« Polices chargées » / « Rôles de police ») ; « Animer » ouvre aussitôt les réglages de la transition
  - [x] Bug trouvé via le panneau de problèmes de Next : un lien déposé dans un lien (HTML invalide, erreur d'hydratation à chaque chargement). Règle ajoutée dans le modèle (déplacement, dépôt, insertion, collage) et rendu dégradé en span ; documents réparés
  - [ ] Sélection multiple → M4

### M4 · Mode Écriture (D18) — fait le 8 septembre 2026 (première version)

- [x] Insertion par « / » avec recherche de blocs (sections, structure, contenu, données, composants), à la place d'un bloc vide ou après
- [x] Édition du texte en place dans l'aperçu : un clic place le curseur ; gras, italique, souligné, barré, lien (barre sur la sélection, ⌘B ⌘I ⌘U ⌘K) ; Entrée crée le bloc suivant (ou l'élément de liste suivant), Retour arrière sur un bloc vide le supprime et remonte ; le contenu est sérialisé en marques du modèle
- [x] Barre au survol d'un bloc : poignée de déplacement, type (paragraphe, titres, citation), alignement, Style (bascule en Design sur l'élément), insérer après, supprimer
- [x] Glisser pour réorganiser les blocs (poignée ⋮⋮ ou glisser l'élément sélectionné)
- [x] Modèles de section prêts à insérer : Héros, Texte et image, Trois points forts, Appel à l'action, Questions fréquentes (groupe Sections de l'onglet Ajouter et du menu /)
- [x] Style en contexte : inspecteur simplifié en Écriture (alignement, taille, aide) avec « Régler le style en détail » qui bascule en Design sur l'élément ; bouton Style dans la barre de bloc
- [x] Exercice n°2 (8 sept.) : page « Mariages » construite entièrement en Écriture (titre, intro, héros, liste avec Entrée, image et ressource, citation, bouton, FAQ avec gras et lien, appel à l'action, vue de base de données, déplacement par la poignée, vue mobile). Corrigé : classement du menu / (le libellé prime sur la description, plus d'en-têtes de groupe pendant la recherche) et mots-clés par bloc (faq, cta, photo, puces…) ; bloc Citation ; Échap garde le texte ; Entrée sur un élément de liste vide sort de la liste ; un lien posé sur une sélection perdait son texte (sérialisation) ; les vues de base de données sont atomiques au dépôt en Écriture (on ne tombe plus dans le modèle de carte) ; Héros et Texte et image passent en une colonne sur mobile ; un modèle qui apporte un h1 devient h2 si la page en a déjà un ; tailles de titres fluides (clamp) dans le thème d'exemple ; marges navigateur remises à zéro pour citation, h5, h6, figure ; un bouton inséré seul ne s'étire plus sur toute la largeur ; la page ouverte est mémorisée au rechargement

### M5 · Données et formulaires

- [x] Édition des bases (8 sept.) : onglet Données (bases, nombre d'entrées et brouillons, modèle de page, création d'une base), vue tableur en dialogue : entrées en lignes, champs en colonnes, cellule par type (texte, nombre, date, oui/non, choix, choix multiples, image et galerie via la bibliothèque, lien, couleur, relation simple ou multiple, texte long en paragraphes), statut publié/brouillon d'un clic, adresse qui suit le titre, suppression avec confirmation ; API `GET/PUT/DELETE /api/sites/:id/entries` (schéma `entry`, testé), état optimiste avec envois en série, aperçu mis à jour en direct
- [x] Gestion des champs (8 sept.) : ajouter, renommer (clé qui suit le libellé tant que le champ est vide), typer, obligatoire, options des choix, cible et cardinalité d'une relation, déplacer à gauche/droite, supprimer (sauf le champ titre) ; les définitions restent dans le document, donc annulables
- [x] Vues (8 sept.) : panneau « Vue de base de données » en Écriture et en Design : base, filtre plat (conditions « et », opérateur et valeur adaptés au type du champ, « l'entrée de la page » pour une relation), tris successifs (jusqu'à trois), limite, texte si vide ; en Design seulement : disposition (grille, liste, défilement horizontal avec accroche) et colonnes ou éléments visibles par point de rupture. Le CSS de disposition vient de la vue (moteur, testé), le style du nœud garde le dernier mot ; le modèle « Vue de base de données » ne pose plus de grille en dur. Reste : pagination (rendu dynamique, M6), calendrier et carte (v1), filtre par paramètre d'adresse dans le panneau
- [x] Modèle de page (8 sept.) : une page devient modèle depuis ses réglages (rôle « Modèle pour <base> », motif d'adresse modifiable) ; sélecteur d'entrée dans la barre pour prévisualiser un modèle avec l'entrée publiée de son choix ; panneau « Données » sur un texte, une image ou un lien placé dans un modèle ou dans une vue (source déduite : `dataSourceFor`, testé) pour lier ou délier le contenu, l'image et son alternative, la cible (page de l'entrée ou champ lien) ; un champ texte long lié se rend en paragraphes (testé). Reste : transformations (format de date, troncature) et liaison des relations traversées depuis le panneau
- [x] Bases, import (9 sept.) : « Importer… » dans le tableur, CSV (séparateur détecté, guillemets, BOM) ou JSON (tableau d'objets, ou export d'Atelier), correspondance des colonnes proposée d'après les libellés et clés (ignorer, nouveau champ texte, identifiant pour mettre à jour, champ existant), aperçu des premières lignes, conversion par type (nombre, oui/non, date, choix par libellé ou valeur, choix multiples, relation par titre, texte long en paragraphes), adresse et ordre automatiques, brouillons ou publiées, un seul envoi. Les images ne passent pas par le CSV (bibliothèque). Export CSV pour toute base
- [ ] Bases, connexion externe (D25 : Notion d'abord, Airtable, Supabase, REST) en lecture avec synchronisation, en v1
- [x] Médias, second temps (9 sept. ; bibliothèque plate, pas de dossiers) : une seule bibliothèque pour tout l'éditeur (bouton de la barre, palette, panneau Image, clic sur une image vide, cellules des bases) ; recherche par nom ou description, tri récentes / par nom, filtre « inutilisées » ; volet de détail : nom, texte alternatif porté par l'image (l'élément surcharge s'il a le sien), dimensions et date d'ajout, « utilisée dans n endroits » (éléments, fonds, référencement, favicon, entrées) avec « aller » vers l'élément ou « retirer » d'une entrée ; remplacer le fichier (même identifiant, tous les usages suivent) ; retirer une image inutilisée, ou toutes d'un coup ; en mode choix, double-clic ou « Utiliser ». Reste : étiquettes libres si le besoin apparaît, suppression du fichier dans le stockage
- [x] **Formulaire de contact fonctionnel** (9 sept., `docs/formulaires.md`) : `POST /api/forms/:formId` (multipart ou JSON, réponse JSON ou redirection `?envoye=`), validation par champ (obligatoire, email, nombre, choix, longueur), piège à robots silencieux et dix envois par minute par adresse, enregistrement comme entrée de la base virtuelle `frm_<formId>`, notification par email via Resend si configuré ; script du site pour l'envoi en place avec message de succès ou d'erreur (absent dans l'éditeur) ; base CSS des champs ; onglet Données → « Messages reçus » par formulaire (compteur des nouveaux, tableau en lecture seule, traité/nouveau, suppression) ; export CSV de toute base ; panneaux Formulaire (message de succès) et Champ (libellé, clé, sorte, aide, obligatoire, choix). Reste : destinataire par formulaire et domaine d'envoi (M6), fichiers joints et réponse automatique (v1)
- [x] Médias (8 sept.) : import d'images depuis l'ordinateur (`POST /api/sites/:id/assets`, plusieurs fichiers, glisser-déposer, adresse web), stockage Supabase Storage (seau `assets`, créé par le schéma ou à la volée) ou `.atelier-data/assets` en développement ; conversion HEIC dans le navigateur (voie native puis heic2any), réduction avant envoi au-delà de 4 Mo ; déclinaisons WebP 480/960/1600/2400 produites par sharp, `srcset` et `sizes` au rendu (D37) ; ressources nommées (`name`) et renommables ; bibliothèque d'images (dialogue) ouverte depuis le panneau ou d'un clic sur une image vide dans l'aperçu ; sélecteur du panneau avec vignettes nommées et tuile d'import

### M6 · Publication — première version le 9 septembre 2026 (`docs/publication.md`)

- [x] Site publié servi depuis l'instantané publié, jamais depuis le document de travail (D35, D36) : route `/s/<sous-domaine>/…`, rendu à la demande par le même moteur, cache par étiquette `site:<id>` invalidé à la publication (régénération à la demande, sans construction)
- [x] Sous-domaine : `settings.subdomain` (fenêtre Publier), réécriture `<sous-domaine>.<ATELIER_SITES_DOMAIN>` → `/s/…` par `src/proxy.ts`, liens racine derrière un sous-domaine et préfixés en repli par chemin ; `marie.localhost:3000` en développement. Le domaine d'Atelier reste à acheter `[?]` ; le certificat viendra du domaine wildcard chez Vercel
- [x] Publier / historique / retour arrière : fenêtre Publier (état en ligne, écart de versions, note, historique, « Remettre en ligne »), API `GET/POST /api/sites/:id/publish` et `POST …/publish/restore`, instantanés `snapshots` (`kind = 'publish'`) avec les entrées, `sites.published_version` et `sites.subdomain` (bloc « publication » du schéma à exécuter sur un projet existant, vérifié par `scripts/check-supabase.mjs`)
- [x] Référencement automatique (D38, D39 en partie) : titre avec suffixe, description, canonique, Open Graph et carte Twitter, `robots` par page (« Indexer »), favicon, `sitemap.xml` (pages fixes et une adresse par entrée publiée), `robots.txt` ; réglages du site (suffixe, description, image sociale, favicon) dans la fenêtre Publier, image sociale et indexation par page dans les réglages de page ; code head et fin de body injectés (D40)
- [x] Polices préconnectées et chargées avec `display=swap`, `color-scheme` annoncé ; reste : bascule clair/sombre selon le système (décision à prendre : réglage du site), performances à mesurer avec Lighthouse une fois déployé
- [x] Code head et fin de body (D40) : fait le 9 sept. avec le « minimum professionnel » (fenêtre Publier → Réglages du site → Code personnalisé, injecté dans le `head` et en fin de `body` du site publié et de l'export) ; reste le code par page, en v1
- [x] Déploiement Vercel du rendu multi-sites (9 sept., `atelier-editor.vercel.app`, `docs/mise-en-ligne.md`) ; reste `[ ]` le domaine wildcard des sites publiés (DNS, `ATELIER_SITES_DOMAIN`) et une mesure Lighthouse sur la production
- [x] Vérifié sur Supabase le 9 sept. : colonnes en place, publication depuis l'éditeur, site servi avec quatorze adresses dans le plan

### Sortie de v0

- [x] Le site de la photographe est en ligne, construit dans Atelier (9 sept. 2026 : `https://atelier-editor.vercel.app/s/site-marie`, publié depuis l'éditeur en ligne, formulaire de contact reçu ; reste les deux domaines, `docs/mise-en-ligne.md`)
- [x] Un deuxième site de nature différente est commencé pour vérifier qu'on n'est pas enfermé (D10) : « Maison Aurèle », restaurant, 9 sept. 2026 (`packages/model/src/sample-restaurant.ts`)

---

### Comptes et tableau de bord — première version le 9 septembre 2026 (avancé depuis v1, D50)

- [x] Connexion par lien magique (Supabase Auth via `@supabase/ssr`) : page `/connexion`, envoi côté serveur après vérification des adresses autorisées (`ATELIER_ALLOWED_EMAILS`), retour `/auth/callback`, déconnexion ; `src/proxy.ts` protège l'éditeur, l'aperçu et l'API, laisse publics les sites publiés, les formulaires, les fichiers ; sans variables publiques Supabase, accès libre (développement)
- [x] Multi-sites : `sites.owner`, `listSites(owner)`, création (`POST /api/sites`, site vierge `blankSite` testé ou exemple photographe), suppression, éditeur à `/sites/<id>`, aperçu à `/preview/<id>/…`, formulaires à `/api/forms/<id>/<formId>`, garde du propriétaire dans chaque route d'un site
- [x] Tableau de bord `/` : sites du compte avec état de publication, changements à publier, adresse en ligne, nouveau site, suppression
- [x] Rôles et partage d'un site entre comptes (D51, 9 sept.) : propriétaire, éditeur, rédacteur ; table `site_members`, invitation depuis la fenêtre Publier, rédacteur cantonné à l'Écriture et aux contenus, publication des contenus seule ; reste `[ ]` l'email d'invitation et le gabarit d'email de connexion en français (Resend SMTP)

## v1 · Utilisable pour un client

État au 11 septembre 2026 — déjà fait pendant la v0 : panneau SEO et redirections (D39), code head/body par site (D40), première tranche des interactions et états (D31–D33 : système d'animation à images-clés et déclencheurs, apparitions, cascade, clic, survol, variantes, parallaxe, bandeau, carrousel, compteurs), espace de travail et rôles (D50–D51), import et export CSV/JSON. Reste : domaine personnalisé et wildcard, export Next.js (D15), API des bases (D46), email et webhooks (D43–D45), import Figma structurel (D26), interface bilingue, email d'invitation. Plus les suites de l'audit UX (`docs/audit-ux-2026-09.md`) : multi-sélection, un seul modèle responsive pour boîtes et vues, reparentage au clavier, `Field` / `PropRow` étendus.

Domaine personnalisé · export Next.js (D15) · panneau SEO et redirections (D39) · code head/body (D40) · interactions et états (D31–D33) · espace de travail et rôles (D50–D51) · import et export CSV/JSON, API des bases (D25, D46) · email et webhooks (D43–D45) · import Figma structurel (D26) · interface bilingue (D05) · audit d'usage n°2 avec un vrai client.

## v2 · Vendable

Composants code et SDK (D41, D58) · bibliothèque de composants (D42) · IA sur le modèle (D53–D54) · import Figma fonctionnel et itératif (D27–D28) · multilingue (D48) · bases externes Notion et Airtable (D25) · intégrations Notion, Sheets, Slack (D45) · temps réel et commentaires (D52) · formulaires avancés · plans payants (D55).

## v3 · Plateforme

Espaces membres (D49) · synchronisation Git des composants · contributions à la bibliothèque · plan Espace agences (D55) · export Astro · plugin Figma (D30) · e-commerce : décision à reprendre.

---

## Décisions prises en route

- 6 sept. — **Identité visuelle de l'éditeur** : dense et sombre, outil pro, sans faire peur au débutant. Le système de design précède les panneaux (M1 avant M3).
- 6 sept. — **Nom** : « Atelier » reste le nom de code. Une seule constante dans le code (`PRODUCT_NAME`), pas de nom en dur ailleurs, pour changer plus tard sans coût.
- 6 sept. — **Hébergement des sites publiés** : Vercel en v0 (une application de rendu multi-sites, région Europe, régénération à la demande), derrière une interface `Publisher`. Réévaluer Cloudflare (domaines clients avec certificats automatiques, bande passante moins chère) au moment des domaines personnalisés en v1.

- 8 sept. — **Frontière du mode Écriture** : en Écriture on décide du contenu et de son ordre, en Design de l'apparence. Test pour chaque future fonction : si l'action peut casser la cohérence visuelle du site (valeur libre avec unité, couleur, bordure, espacement, grille), elle va en Design ; sinon (type de bloc, modèle de section, ordre, lien, image, séparateur) elle a sa place en Écriture. Écriture n'est pas un mode débutant mais le mode du propriétaire du site, à qui le freelance livre sans risque. Sa richesse vient des modèles et des variantes de styles partagés (des choix, pas des réglages), pas de réglages supplémentaires. À reporter dans le document de cadrage (D60) si Antoine le souhaite.
- 8 sept. — **Séparateur sans orientation** : un séparateur suit son conteneur, horizontal dans une colonne, vertical dans une rangée, point de rupture par point de rupture (règle dans le moteur CSS, testée, notée dans la spécification section 11).

- 14 sept. — **« Après » dans la rubrique Animation** : un élément qui démarre après un autre est une piste de l'animation qui lance cet autre (une seule animation, un seul délai), réglée par « Démarre · Délai · Rejouer » sous l'effet ; validation par test simulé avant de vraies personnes (`docs/plan-usage-animation-2026-09.md`, cadrage § 7).

## Décisions ouvertes

- `[?]` Nom définitif du produit et domaine des sous-domaines (avant M6 · Publication).

## Revue globale du 8 septembre 2026

Cinq examens (architecture, charge, UI/UX, cohérence produit, sécurité) consolidés dans `docs/revue-globale-2026-09.md`, avec un plan en trois temps : A. avant toute mise en ligne (pagination des entrées qui perd des données au-delà de 1 000, sécurité de la mise en ligne, opérations jamais perdues, allègements de transport) ; B. points de passage anti-dette (migration de schéma, protocole d'aperçu typé, tests communs aux dépôts, intégration continue, régie UI) ; C. v1 fidèle au positionnement (export du code en premier, composants, minimum professionnel, rôles, images hors du document). Dette sans domicile récupérée : sélection multiple et texte riche dans les champs (ex « M4 bis »), destinataire par formulaire et domaine d'envoi.

## Journal

- 23 sept. 2026 (suite 8) — **Versions : journal court, jalons longs** (question d'Antoine : « version 1011 » sur un site simple). En TDD (`replace-site.test.ts` au modèle, contrat du dépôt, `history.test.ts`). 1) Le compteur disparaît de l'interface : badge « Enregistré à 14:32 » (`savedAt` du document), « En ligne · N modifications depuis la mise en ligne ». 2) Points de reprise automatiques : `SiteStore.checkpoint` (un par jour de travail au plus, jamais en double d'une publication, trente gardés), déclenché par la route des changements au plus une fois par heure ; compactage du journal à chaque point et publication, `compact_changes` révisé (garde les 500 derniers lots, ne retire que ce qu'un instantané couvre ; bloc SQL à relancer, `docs/supabase.md`). 3) Historique lisible dans la fenêtre Publier (`historyRows`) : publications et points de reprise datés, « Remettre en ligne » pour une publication, ↺ « Reprendre comme version de travail » pour les deux (`POST …/publish/resume`, `planReplaceSite` : un changement ordinaire, annulable).
- 23 sept. 2026 (suite 7) — **Infobulles du rail** (constat d'Antoine : elles débordaient à gauche de la fenêtre). `Tooltip` gagne un côté « right » (`data-side`), le rail l'utilise ; test dans `left-rail.test.tsx`.
- 23 sept. 2026 (suite 6) — **Interface, chantier 3 : l'animation est un outil, pas un mode (D60)**, en TDD (`animation-tool.test.tsx`, `canvas-bar.test.tsx`). Le quatrième onglet disparaît : D17 retrouve ses trois modes. Un tiroir sous le canevas (`data-animation-drawer`, 200 à 640 px, hauteur mémorisée) s'ouvre depuis « Ouvrir la ligne de temps » (rubrique Animation de l'élément), « Animer « X » », le bouton de la barre du canevas ou la palette. `AnimationModePanel` en deux colonnes (déclencheurs et bibliothèque ; ligne de temps ou invitation), `Timeline` en deux colonnes (scène et pistes ; réglages de la piste et de l'image-clé) : plus de scène collante ni de panneau de droite élargi. L'aperçu reçoit « animate » (sélection seule, instant montré) tant qu'une ligne de temps est ouverte ; les calques marquent animés et déclencheurs quand le tiroir est ouvert. Docs : D60 dans `decisions.md`, révision en tête du § 4.1 du cadrage, `fonctionnel.md` § 1.1 et § animations, `architecture.md`, `CLAUDE.md`.
- 23 sept. 2026 (suite 5) — **Interface, chantier 2 : rail d'icônes à gauche** (`components/LeftRail.tsx`, `left-rail.test.tsx`). Les cinq onglets textuels du panneau gauche deviennent une colonne d'icônes de 44 px (Pages, Calques, Ajouter, Données, Thème ; Calques et Thème absents en Écriture), le panneau de 280 px s'ouvre à côté avec son titre et un bouton « Replier », l'icône active le replie. En bas du rail, les outils du site : images, palette ⌘K, réglages. Le mode concentration cache le rail aussi.
- 23 sept. 2026 (suite 4) — **Interface, chantier 1 : barre du haut en trois zones et barre du canevas** (décision d'Antoine après le débordement). En TDD (`canvas-bar.test.tsx`). Barre du haut : le site › la page, les modes au centre, annuler/rétablir, l'état, Aperçu, réglages et Publier à droite ; elle tient dans 600 px. Nouvelle barre du canevas (`components/CanvasBar.tsx`, 36 px au-dessus de l'aperçu, à requêtes de conteneur) : entrée affichée d'un modèle de page, largeur (préréglage et pixels), « Vous modifiez : … » en Design, réduction en %, grille, comparaison mobile, clair/sombre, concentration. Elle rétrécit avec le canevas au lieu de pousser « Publier ». La pastille flottante « Vous modifiez » disparaît, le badge de la barre la remplace. Suite : chantier 2 (rail d'icônes à gauche), chantier 3 (animation comme outil, tiroir sous le canevas).
- 23 sept. 2026 (suite 3) — **Débordement de la barre du haut** (constat d'Antoine : « Publier » hors de l'écran). La barre demandait 993 px ; sous cette largeur, le groupe de droite passait derrière le bord. Correction : sous 1200 px, grille, panneau gauche et concentration quittent la barre (ils restent dans la palette ⌘K, qui gagne deux entrées, et au clavier) ; sous 1100 px, le choix de largeur aussi (la poignée du canevas suffit) ; barre en `overflow-hidden`, modes et groupe de droite non rétrécissables. Vérifié à 820, 900, 1024 et 1200 px : « Publier » reste visible. Réorganisation plus large (barre en trois zones, rail d'icônes à gauche, animation comme outil et non comme mode) : proposée à Antoine, en attente de sa décision.
- 23 sept. 2026 (suite 2) — **Formulaires dans l'export**, en TDD (`render.test.tsx`, `export.test.ts`, `forms-route.test.ts`). Avant : l'archive gardait `action="/api/forms/…"` en relatif et la route refusait les autres origines, donc aucun formulaire ne marchait hors d'Atelier. Maintenant : `props.endpoint` (« Service d'envoi » dans le panneau Formulaire) pour poster chez Formspree ou ailleurs sans dépendre d'Atelier ; sinon l'export vise Atelier en adresse absolue (`formsOrigin` du contexte de rendu, origine de l'instance qui exporte) et la route répond en CORS ouvert avec `OPTIONS`, retour sans script vers la page d'origine même sur un autre domaine (`returnUrl`, `corsHeaders` dans `lib/forms.ts`). README de l'archive formulaire par formulaire.
- 23 sept. 2026 (suite) — **Bilan avant publication**, en TDD (`packages/model/test/checkup.test.ts`, `apps/editor/test/checkup-section.test.tsx`). `checkup(site)` dans le modèle : treize règles calculées à partir du document seul (textes alternatifs, déclinaisons, image prioritaire du premier écran, titres et descriptions, doublons de titre, titre de niveau 1, liens vers une page supprimée, fichiers de police, « au chargement » sous le premier écran, sous-domaine, favicon, image de partage), chaque constat avec son niveau (« à corriger » / « à regarder »), sa phrase et sa cible ; `checkupSummary` pour la phrase de comptes. Section « Bilan avant publication » dans la fenêtre Publier (`CheckupSection.tsx`) : comptes, liste à la demande, clic → page et élément (`goTo` de l'EditorShell, partagé avec la bibliothèque) ou onglet Réglages. Rien ne bloque. Au passage, les sites d'exemple corrigés par leur propre bilan : textes alternatifs des photos du restaurant, image du héros prioritaire. Reste, décidé : la mesure en ligne (Lighthouse sur le site publié) n'est pas dans l'outil ; le bilan local suffit pour la v1.
- 23 sept. 2026 — **Optimisations avant la v1, mesurées.** Compilation de production et Lighthouse (mobile, débit simulé) sur le site publié d'exemple : 89 → **100** en performance, premier rendu 3,0 s → 0,8 s. Trois corrections en TDD : feuille Google Fonts non bloquante (`html-document.ts` : `media="print"` puis `onload`, repli `noscript`), `fetchpriority=high` sur une image prioritaire (`render.tsx`), en-têtes du site publié `s-maxage=60, stale-while-revalidate=600` (`publishedHeaders`, une publication visible au plus une minute après ; documenté dans `docs/publication.md`). Paquets : `sideEffects: false` sur `@atelier/model` et `@atelier/renderer`, ce qui élague les sites d'exemple et zod hors des paquets du navigateur : éditeur ≈ 363 → ≈ 290 Ko gzip, aperçu ≈ 250 → 171 Ko gzip. Non traité, à décider : le point 14 de la revue (images hors du document, opérations vers l'aperçu au lieu du document entier, éditeur chargé par page), nécessaire au-delà d'une cinquantaine de pages ou de cinq cents images par site.

- 20 sept. 2026 (suite 3) — **Lot 9** (vague 5, § 5 du rapport), en TDD : `planChainInOrder` (modèle) et bouton « Enchaîner dans cet ordre » sur la vue Scène ; barres de piste qui se tirent dans la ligne de temps ; réglages de la piste active avant la liste des éléments ; tête de lecture à la fin d'une piste qu'on vient de remplir ; phrase de résumé du bloc collant sur deux lignes (dépliable) ; « Lire » en toutes lettres et barre d'espace ; « Une piste pour « X » » pour l'hôte ; « n'a pas encore d'effet ». Tests : `appearance` (modèle), `scene-overview`, `timeline-layout`, `timeline`. Non retenu du § 5 : la durée d'une animation neuve qui suivrait ses pistes (la longueur affichée la suit déjà ; la perception de P5 venait du champ « Durée » à 1 000). Prochaine étape décidée : fin des vagues simulées, test avec une vraie personne.

- 20 sept. 2026 (suite 2) — **Validation par l'usage : vague 5, la scène seule** (`docs/validation-animation-2026-09-vague5.md`, pièces dans `docs/validation-animation-2026-09-vague5/`). Test simulé court sur `17505d3` (lot 8) : T4 en première position, cinq participants, sept codages, aucun incident. **T4 reste en échec chez 5 sur 5**, mais le profil change : la cause n° 1 de la vague 4 (le groupe des chiffres) est levée (3 sur 5 l'atteignent, 26 éléments animés sur 30), les aides du lot 8 sont trouvées sans être cherchées par 4 sur 5 ; la cause n° 2 ne l'est qu'à moitié : la vue « Scène » dit la vérité mais ne se règle pas, l'ordre (C3) reste à 0 sur 5 et le champ « Départ » de la piste est rogné par la nouvelle liste des éléments. Lot 9 proposé au § 5 du rapport : régler l'ordre là où on le voit (vue Scène actionnable, barres de piste qui se tirent), « Départ » avant la liste, bloc collant de hauteur fixe, tête de lecture à la fin d'une piste remplie, durée d'une animation neuve qui suit ses pistes. Décision du point du 20 septembre maintenue : après le lot 9, arrêter les vagues simulées et tester avec une vraie personne.

- 20 sept. 2026 (suite) — **Lot 8** (vague 4, § 6 du rapport), en TDD. Temps 1, désigner le groupe depuis un enfant : `siblingGroup` et `planGroupAppearance` (modèle), champ « Ses voisins » dans la rubrique Animation d'un chiffre ou d'une carte, rubrique Animation avant l'écran « Composant » sur une occurrence (aide réécrite), liste « Éléments de la scène » dans la ligne de temps (`sceneElements`, un groupe d'un coup). Temps 2, la scène entière : `lib/scene.ts` (`sceneOf`, `sceneSentence`, `sceneContainerFor`) et `SceneOverview` sur une section ; départs, fins, total, phrase de l'ordre, avertissements (part avant celui qui le précède ; lancements séparés), éléments immobiles ; lien depuis un élément. Temps 3, faire découvrir : « Pareil pour … » nommé et placé sous l'effet, « Tester sur le site » avant la première animation et en bouton dans la ligne de temps, phrase sur la tête de lecture. Temps 4, mots : « Quand la souris passe dessus », « les N éléments de », « Intervalle », secondes à côté des ms, durée saisissable (`planAppearanceDuration`), corbeille pour retirer un déclencheur. Tests : `appearance` (modèle), `quick-animations-group`, `scene`, `scene-overview`, `discoverability`, `words`, `timeline`. Vérifié dans l'éditeur sur les sites de P3 et P5 de la vague 4. Une vague 5 doit mesurer U2 et U4 sur cette version.

- 20 sept. 2026 — **Validation par l'usage du parcours d'animation : vague 4** (`docs/validation-animation-2026-09-vague4.md`, pièces dans `docs/validation-animation-2026-09-vague4/`). Test simulé mené les 16 et 17 septembre sur `99fb42a` (lot 7), mêmes personas, missions, critères et ordre que les vagues 2 et 3, cinq séances en parallèle, sept codages, synthèse à l'aveugle, comparaison avant / après. **5 critères atteints sur 8** (U1, U3, U5, U6, N2 ; 3 en vague 3) : 20 réussites complètes sur 25, T2 et T3 à 5 sur 5, aucune aide ni abandon ni collatérale, aucun éclair ni phrase contredite. T4 reste à 0 sur 5 (U2, U4), couverture 24 sur 30 contre 14 ; N1 manqué de deux récits. Causes de T4 inchangées : le groupe des chiffres hors d'atteinte au clic et aucune vue de la scène entière ; les trois aides du lot 7 pour composer n'ont été trouvées par personne. Deux défauts nouveaux de la ligne de temps (scène collante, interrupteurs à l'envers) et le triangle sans mot, corrigés le 19 septembre. Lot 8 proposé au § 6 du rapport. Dispositif : rattachement du navigateur de test après une interruption d'une nuit (limite de session), budget de 40 actions non arrêté chez deux participants (statuts lus dans le site, comptes de T4 indicatifs).

- 19 sept. 2026 (suite) — **Trois corrections issues de la vague 4 des tests simulés** (séances menées les 16 et 17 septembre sur `99fb42a`, 20 réussites et 5 échecs sur 25, tous en T4 ; synthèse et rapport non écrits, matériel hors dépôt), en TDD : la scène collante de la ligne de temps recouvrait l'en-tête « Piste » et le champ « Départ » dès cinq pistes (une participante y a perdu sa mission) : les réglages de la piste active sont ramenés sous la scène quand elle change (`animation/reveal.ts`) ; les interrupteurs du déclencheur (« Rejouer », « Quand la souris part », « Clic suivant », « Souris dessus ») portent le mot de ce que coche la case au lieu de l'état courant lu à l'envers (« une seule fois », « se coupe ») ; le triangle « Jouer » des choix rapides et de la section Animations devient un bouton « Voir l'effet » (jamais cliqué par les débutantes : « un dessin tout seul »). Tests : `trigger-settings`, `quick-animations-play`, `timeline-reveal`.

- 19 sept. 2026 — **Architecture : ce qui était faux et ce qui manquait.** Un schéma généré depuis les fichiers avait suggéré cinq « services », un état publié hors façade, ni export ni médias, et aucun runtime côté visiteur ; vérification : les quatre premiers points étaient des artefacts du schéma (un seul processus, instantanés dans le dépôt, export `.zip` et médias derrière `AssetStorage`), le cinquième était vrai et la documentation le contredisait (« sans JavaScript d'Atelier »). Fait, en TDD : **conflit de version reposé** au lieu de figer l'éditeur (`lib/rebase.ts` : relecture du serveur, rebasage tout ou rien des opérations en attente, renvoi unique, annulation qui repart de zéro, message ; gel seulement si une opération n'a plus de sens ou si le renvoi reconflit ; `test/rebase.test.ts`, `test/use-document.test.tsx`) ; **`docs/architecture.md`** (trois paquets, un processus, quatre runtimes dont le script du site publié, journal et conflits, dépôt, publication, fragilités connues), lié depuis `CLAUDE.md` ; `docs/fonctionnel.md` § 1.9 et `docs/publication.md` corrigés. Reste, noté dans la carte : découper `EditorShell.tsx` et `LivePreview.tsx` par petites extractions autour de `preview-protocol.ts`.

- 16 sept. 2026 (suite 4) — **Hygiène** : `Timeline.tsx` découpé (`TrackSettings.tsx`, `KeyframePanels.tsx`, `TriggerSettings.tsx` ; la scène et la sélection d'images-clés restent dans `Timeline.tsx`, 353 lignes) ; les règles des apparitions écrites en neuf points dans le contrat (§ 8.4), là où elles n'étaient qu'un paragraphe.
- 16 sept. 2026 (suite 3) — **Lot 7, temps 3 : réduire le coût de la scène** (vague 3, § 6.8 à 6.10), en TDD. Désigner un bloc intermédiaire : ⌥-clic remonte d'un niveau dans l'aperçu (`climbSelection`, 1 test) et une flèche « bloc qui contient » en tête de l'inspecteur. Moins de gestes : « Pareil pour les éléments qui suivent » (`planAppearanceCascade`, 2 tests : les voisins sans apparition rejoignent l'animation, chacun avec le précédent + 120 ms) ; « Reprendre « piste » pour lui » dans la ligne de temps (`planDuplicateTrack`, 1 test). Phrase de résumé : pistes vides dites, sortie du survol dite, secondes à côté des millisecondes (`formatDuration`, 3 tests). Vérifié dans l'éditeur (⌥-clic sur un chiffre : Colonnes, puis la section).
- 16 sept. 2026 (suite 2) — **Lot 7, temps 2 : aligner les deux entrées** (vague 3, § 5.4 et § 6.5 à 6.7). Mode Animation : les réglages du déclencheur (`TriggerSettings`, extrait dans son fichier) sont dans la scène de la ligne de temps, sous la phrase de résumé (Quand, Délai, Rejouer, Au départ, Clic suivant, Axe, Plage) et ne sont plus répétés dans la liste quand la ligne de temps est ouverte ; un élément qui arrive avec un autre le dit (« Arrive avec « Plats » · Fondu en montant », `arrivesWith`, 1 test) avec « Voir cette animation » ; l'attente d'une piste enchaînée se lit « délai » (plus « écart »). Inspecteur : pour une vue, un formulaire ou un champ, la rubrique Animation précède les réglages techniques. Vérifié dans l'éditeur (bascule « Rejouer » depuis la scène, ⌘Z).
- 16 sept. 2026 (suite) — **Lot 7, temps 1 : corriger le faux** (vague 3, § 6, défauts 1 à 4), en TDD. Modèle : la migration 2 → 3 donne un identifiant libre à chaque animation même quand deux éléments copiés partageaient un run (`an_<id>_2`), et `repairAnimations` répare à la lecture les documents déjà convertis (doubles identiques fusionnés, différents renommés) ; une copie d'élément renouvelle les identifiants de ses déclencheurs (`cloneWithNewIds`). Éditeur : un champ chiffré validé par Entrée ne produit plus qu'une opération (la perte de focus qui suit ne revalide pas ; test React dans happy-dom), et ⌘Z défait bien le geste. Rubrique Apparition : `appearanceOptions` (`lib/appearance-options.ts`, 4 tests) dit « Avec « Plats » · Fondu en montant » comme premier choix, non sélectionnable puisqu'il n'écrirait rien ; un préréglage s'applique à la liste pour une carte, à l'élément sinon ; `Select` accepte des options désactivées. Rendu : une piste en attente dont toutes les propriétés sont déjà animées sur la même cible remplit seulement vers l'avant (`trackFill`, CSS et script : `f` sur la piste), une apparition garde son état de départ grâce à l'opacité qui n'est qu'à elle. Contrat § 8.4 mis à jour. Tests : 151 modèle, 66 rendu, 60 éditeur.
- 16 sept. 2026 — **Validation par l'usage du parcours d'animation : vague 3** (`docs/validation-animation-2026-09.md`, pièces dans `docs/validation-animation-2026-09/`). Test simulé de validation prévu par le plan (lot 6), sur `08a6d1e`, avec les mêmes personas, missions, critères et ordre de passage que la vague 2 : cinq séances complètes, cinq observateurs indépendants, doubles codages de P3 et P5, synthèse à l'aveugle, puis comparaison avant / après. Dispositif changé en cours de route : le navigateur intégré étant inutilisable fenêtre masquée (T1 de P3 invalidée), les séances passent sur un **Chrome sans fenêtre** piloté par le protocole DevTools (capture qui attend le rendu, loupe réelle) ; deux autres incidents consignés (veille de la machine, T1 de P5 invalidée et séance reprise de zéro ; coupure réseau pendant T5 de P5). **Résultat : 3 critères pré-enregistrés atteints sur 8.** Progrès : « les boutons après le titre » passe de 1 à 4 réussites sur 5, dont 3 sans ouvrir le mode Animation ; aucune aide donnée en 25 missions (3 en vague 2) ; l'« éclair » avant l'apparition a disparu des visites instrumentées ; la sélection du texte ou de l'image contenus tombe de 5 participants à 1 ; T1 et T5 restent à 5 sur 5. Inchangé : la scène de « La maison » (échec chez 5 sur 5), et le conteneur d'un groupe reste inatteignable au clic. En retrait : les récits (20 exacts sur 25 contre 24), le moment d'une animation existante depuis le mode Animation (gravité 4, 2 échecs), et la couverture de T4 (14 éléments animés sur 30 contre 24 — le lot 5 a supprimé le raccourci involontaire par lequel trois participants réglaient le composant sans le savoir). Défaut rendu visible par la correction du dispositif : `NumberInput` valide deux fois avec Entrée (Ctrl+Z n'annule que le doublon, une piste peut être corrompue, un abandon en découle). Suite : lot 7 (défauts d'abord, puis alignement du mode Animation sur la rubrique du panneau, puis désigner un bloc et réduire le nombre de gestes). Aucun code modifié dans cette session.

- 15 sept. 2026 (suite) — **Revue de code des lots 0 à 5 et corrections.** Une revue indépendante établit 16 défauts (scènes détruites par des gestes courants, enchaînement déduit des positions, boucle et apparition sur un élément enchaîné, occurrences de composant, sélection). Correction de fond : l'enchaînement est enregistré dans le modèle (`Track.start`, spécification § 8.4 ; `layoutTracks`, `planTracks`), `appearance.ts` réécrit, suppression sûre (`planRemoveNode`), détachement qui reporte les animations ; rendu : règles d'animation réunies par cible, occurrences en `.racine.occurrence`, script qui ne retire que ce qu'il joue, pistes limitées à leur occurrence, emplacements parcourus ; éditeur : sélection affinée, suites protégées (« + N »), départ enchaîné affiché. 20 tests ajoutés, 4 réécrits. Test simulé de validation suspendu : fenêtre de l'application réduite, clics du participant à côté (P3-T1 invalidée).
- 15 sept. 2026 — **Lots 2 à 5 des corrections par l'usage.** Un élément animé par un autre le dit (« Avec « Plats » », phrase, « Régler sur « Plats » ») au lieu d'« Aucune » ; « une à une » ne s'affiche plus quand les cartes partent ensemble. Apparitions « Montée avec rebond » et « Zoom avec rebond ». Une occurrence de composant peut apparaître et être visée par une piste (rendu). « Démarre : quand « La maison » entre dans l'écran » lance toute une scène par sa section. Mode Animation : apparition ouverte sur son état visible, « Comment lire cet écran », « Voir ses animations / L'ajouter à cette animation » pour un élément hors de l'animation ouverte, « Animations du site » par page et changement de page signalé. Canevas : un clic désigne d'abord le bouton, la carte, l'occurrence ou la pastille, le suivant descend (`lib/selection.ts`). Portée d'un réglage de composant dite. Vérifié : T2 (carte → « Avec « Plats » ») et T4 (scène complète composée depuis la rubrique, rendu conforme aux critères C1 à C6). 9 tests ajoutés.
- 14 sept. 2026 (suite 2) — **Lot 1 : « après » depuis la rubrique Animation.** `appearance.ts` (modèle, 18 tests) : l'apparition d'un élément se lit où qu'elle soit (sur lui ou dans l'animation d'un autre élément), avec ce qui la fait démarrer (lui-même, après, en même temps) et son délai ; « Démarre après « Titre » » range la piste dans l'animation du titre, à sa fin ; vitesse, délai, effet et détail gardent l'enchaînement ; retirer le referme. Rubrique « Animation » : Apparition · Vitesse · Démarre · Délai · Rejouer · phrase (« …, puis « Réserver une table » (fondu en montant) de 1 120 à 1 620 ms »). Vérifié sur l'état de départ de T3, dans l'éditeur et sur le site (boutons avec délai de 1,12 s, cachés jusqu'à leur départ). Documents : spécification § 8.4, cadrage § 7, fonctionnel, plan.
- 14 sept. 2026 (suite) — **Corrections par l'usage, plan et lot 0.** Plan `docs/plan-usage-animation-2026-09.md` : critères de réussite pré-enregistrés (U1 « boutons après le titre » 4 sur 5 dont 3 sans le mode Animation, U2 scène 2 designers sur 3, U3 diagnostic 4 sur 5, U4 rejeu et dépassement 3 sur 5, U5 aucun abandon dans le mode Animation, U6 sélection, N1 non-régression, N2 pas d'éclair), principes, six lots, test simulé de validation. Décisions d'Antoine : « après » par enchaînement dans une même animation (solution A), validation par test simulé. Lot 0 : plus d'**éclair** à l'entrée dans l'écran (le script ne retire l'animation CSS en pause qu'à l'entrée, 4 tests) ; une **boucle au chargement** sur un élément qui a aussi une apparition ne s'arrête plus à son entrée dans l'écran (défaut trouvé par les tests) ; phrase « les enfants … ensemble » sans décalage ; **vitesse** : l'option active est encadrée, libellés entiers, « Sur mesure : 500 ms » quand aucune ne correspond (c'était le cas de l'état de départ de T3). PR13 (Entrée) requalifié en artefact du dispositif (la touche « Return » de l'outil envoie une touche vide) ; S15 probablement le moment des captures.
- 14 sept. 2026 — **Tests utilisateurs simulés du parcours d'animation** (`docs/tests-simules-animation-2026-09.md`, pièces dans `docs/tests-simules-animation-2026-09/`), demandés par Antoine. Cinq participants simulés (2 débutants, 3 designers ; agents Opus 5 et Sonnet 5 jouant chacun une persona, écran vu par captures), protocole pré-enregistré conçu à l'aveugle (dix hypothèses à seuils, critères lus dans le site enregistré et par visites instrumentées), modérateur en script fermé, observateurs sans les hypothèses, deux séances doublement codées, synthèse à l'aveugle puis comparaison séparée avec l'audit n°5 ; version `fc1764d` (après R1 à R4). Première vague requalifiée en pilote (incidents du navigateur intégré), deuxième vague complète. Résultats : poser un effet tout prêt réussit (T1 et T5 5 sur 5, titre ralenti 5 sur 5) ; composer échoue (scène T4 0 sur 5, « boutons après le titre » 1 sur 5) ; diagnostic d'une animation existante 2 sur 5. Problèmes retenus : pas d'« après » ni d'attente dans le panneau (4 sur 5, gravité 4 ; « Départ » n'existe qu'en mode Animation), mode Animation qui fait reculer les débutants, « Aucune » affiché sur une carte animée par sa liste, « Animations du site » vers l'homonyme d'une autre page, et 10 autres. Les cinq fonctions cherchées et non trouvées (attente, rejouer, changer le « Quand », enfants un à un, ressort) existent toutes en mode Animation. La phrase de résumé (R2) est ce qui marche le mieux (5 sur 5, 24 récits du visiteur sur 25 exacts). Constats techniques : « éclair » avant l'apparition à l'entrée dans l'écran sur le site (`interactions.ts`), phrase « un à un » sans décalage réel, encart « Comment ça marche » (R3) jamais affiché quand on arrive par « Ouvrir dans le mode Animation » sur un élément animé. Plan consolidé en 10 actions (1 « après » là où l'on pose l'effet ; 2 le site joue ce que l'éditeur annonce ; 3 mode Animation abordable…) ; R5 et R7 à reformuler, R10 confirmée, R6 et R8 sans appui. Tout reste à confirmer avec de vraies personnes (cinq questions prioritaires en section 15). Aucun code modifié.
- 13 sept. 2026 (suite 15) — **Audit n°5, R4 : tester sur le site.** « Tester sur le site » dans la ligne de temps (après la phrase de résumé) et sous les choix rapides (Écriture, Design) ouvre l'onglet Aperçu avec `?voir=<élément>` (`lib/test-on-site.ts` : `testOnSiteUrl`, `revealForTest`, 3 tests dans un DOM simulé) : l'élément, s'il est plus bas, est placé juste sous l'écran puis amené au centre en défilant, et un bandeau invite à le survoler ou le cliquer. Vocabulaire : « Jouer dans le canevas », pioche « dans le canevas ou les calques », l'aide des effets continus renvoie à l'onglet Aperçu au lieu de l'ancien « Voir ». Vérifié : adresses ouvertes depuis la ligne de temps et Design, bandeau dans l'onglet Aperçu ; le défilement automatique n'est pas observable dans le navigateur intégré masqué (document caché), il est couvert par les tests. R1 à R4 de l'audit n°5 faits ; restent R5 à R11 et l'essai avec des utilisateurs.
- 13 sept. 2026 (suite 14) — **Audit n°5, R3 : le modèle est dit à l'entrée du mode Animation.** Encart « Comment ça marche » en trois temps (quand, ce qui bouge, comment), repliable ; bouton « Animer « élément » » sur un élément sans animation (`planAnimateElement`, testé : déclencheur à l'entrée dans l'écran, piste de l'élément prête à remplir) ; « Déclencheurs · … » devient « Animations lancées par … », « Ajouter un déclencheur » devient « Lancer une animation depuis … » avec l'explication « … lance l'animation ; vous choisirez ensuite ce qui bouge » ; guide et état vide reformulés. Vérifié dans l'éditeur.
- 13 sept. 2026 (suite 13) — **Audit n°5, R2 : une phrase dit ce qui va se passer.** `summarizeAnimation` (`lib/timeline.ts`, 3 tests) : quand (chargement, entrée dans l'écran, survol, clic, défilement de l'élément ou de la page, souris), délai, ce qui bouge et à quel moment (préréglage sur l'élément lui-même dit par son nom ; sinon chaque élément, enfants un à un, lettres, décalages), en millisecondes ou en parts du parcours, et la fréquence (une seule fois, à chaque passage, retour au départ de la souris, bascule, boucle). Affichée sous chaque choix rapide (Écriture, Design) et en tête de la ligne de temps ; vérifié dans l'éditeur.
- 13 sept. 2026 (suite 12) — **Audit n°5, R1 : la durée est la vitesse.** Changer « Durée » dans la ligne de temps met toute l'animation à l'échelle, images-clés et décalages compris (`planScaleAnimation`, testé) : vérifié, 800 → 1 600 ms place la fin à 1 600 ms. Choix rapides : vitesse Rapide · Normale · Lente (`QUICK_SPEEDS`, `quickSpeed`, `planQuickSpeed`, testés), l'animation reste son préréglage et changer de préréglage garde la vitesse ; vérifié en Écriture (Fondu lent à 1 120 ms, joué dans l'aperçu).
- 13 sept. 2026 (suite 11) — **Audit n°5 : parcours et valeur des animations** (`docs/audit-parcours-animation-2026-09.md`), demandé par Antoine à la place de la suite des corrections d'interface. Parcours cognitifs sur sept tâches (débutant, composition, retouche, défilement, survol, vérification et publication, retrait) et lecture « valeur » par profil et par promesse du cadrage. Verdict : promesse tenue pour le débutant ; pour le designer, trois idées du modèle ne sont jamais dites (ce qui lance n'est pas ce qui bouge ; le temps est porté par les images-clés, et « Durée » ne ralentit pas — vérifié ; l'éditeur ne montre pas le rendu réel du défilement, du survol ni de l'entrée dans l'écran). Promesses « un seul endroit pour faire bouger » (huit endroits) et « voir ce qu'on règle » (hors temps) non tenues. 11 recommandations de parcours, dont R1 « Durée » = vitesse, R2 phrase de résumé en langage courant, R3 dire le modèle à l'entrée du mode, R4 « Tester sur le site » ; essai court avec 2 ou 3 personnes proposé avant d'engager R1 à R4. Aucun code modifié.
- 13 sept. 2026 (suite 10) — **Audit n°4, priorité 4.** E1 : panneau de gauche repliable (bouton de la barre du haut, replié par défaut en mode Animation, mémorisé par mode ; les commandes « Afficher … » de la palette le rouvrent) ; panneau Animation réglable de 360 à 720 px (poignée, flèches), mémorisé ; barre du haut resserrée sous 1440 px. Canevas en mode Animation, ligne de temps ouverte : 284 → 584 px à 1024, 540 → 840 px à 1280, 700 → 1 040 px à 1440 ; plus de débordement de la barre dans les trois modes. M6 : zoom de la ligne de temps ×1 à ×8 (boutons, ⌘ + molette), règle et pistes dans un même défilement horizontal, repères espacés selon la largeur réelle (`rulerTicks(longueur, zoom, largeur)`, `nextZoom`, testés). Non fait : bandeau bas sous 1280 px (inutile une fois le panneau de gauche replié), colonne des pistes redimensionnable. Restent de l'audit : M1, M3, M4, M5, M7, M8 et les mineurs m1, m2, m3, m5, m6, m7.
- 13 sept. 2026 (suite 9) — **Audit n°4, priorité 3.** E4 : section **Mouvement** en tête des réglages d'une image-clé (`animation/MotionPanel.tsx` : opacité, décalage, échelle, rotation, flou ; Effets et Apparence repliés dessous) ; scène de la ligne de temps collante (nom, lecteur, règle, pistes et ajout restent visibles, pistes défilantes au-delà de 38 % de la hauteur). Mesuré à 1024 × 768 : opacité à 555 px au lieu de 1 152, la règle reste visible après 700 px de défilement. E6 : « ◆ n animations » dans l'en-tête de l'inspecteur ouvre et montre la section Animation(s) en Design et en Écriture ; aide de la mise en forme rapide réduite à une ligne.
- 13 sept. 2026 (suite 8) — **Audit n°4, priorité 2.** E3 : chaque ligne de déclencheur (28 px) s'ouvre d'un clic, chevron et « Modifier » au survol ; en Design le nom ouvre le mode Animation. E5 : un choix rapide se joue une fois dans l'aperçu, bouton « Jouer » en Écriture et en Design. M2 : « lettre par lettre » / « les enfants un à un » sous l'Apparition. M9 : nom par défaut d'après ce qui la lance (`nextAnimationName(site, hôte)`, testé). M10 : la ligne de temps parle en millisecondes (`formatMs`, `tickLabel`, testés). m4 : « Cible », « compter jusqu'au nombre », aide sur « aller-retour ». Vérifié dans l'éditeur.
- 13 sept. 2026 (suite 7) — **Audit n°4, priorité 1 : Entrée dans un champ numérique ne renomme plus le calque** (E2). Les raccourcis de l'éditeur ignorent une touche dont la cible est un champ (`isEditableTarget` dans `lib/keys.ts`, 2 tests dans un DOM simulé), et plus seulement l'élément actif : un champ qui se quitte sur Entrée laissait `body` actif. Vérifié avec de vraies frappes : valeur validée, calque intact.
- 13 sept. 2026 (suite 6) — **Audit n°4 : ergonomie, UX et UI des animations** (`docs/audit-ux-animation-2026-09.md`). Évaluation heuristique, six parcours, revue d'interface et accessibilité, mesures à 1440, 1280 et 1024 px. Verdict : le modèle d'interaction tient et l'accessibilité de base est saine ; ce qui freine est spatial. 6 points majeurs : canevas à 284 px (19 %) à 1024 et 540 px à 1280 avec une ligne de temps ouverte ; **défaut de l'éditeur** : Entrée dans un champ numérique valide puis renomme le calque sélectionné (le champ se quitte, le raccourci global Entrée voit `body` actif) ; entrée peu visible pour ouvrir une animation (ligne de 17 px) ; opacité à 1 152 px sous la ligne de temps ; choix rapides sans aperçu ; sections Animation en bas des inspecteurs. 10 points moyens, 7 mineurs, priorités proposées. Rien n'est corrigé dans ce lot.
- 13 sept. 2026 (suite 5) — **Suites de l'audit d'usage n°3, recommandations retenues par Antoine.** Créer une animation devient guidé (« Nouvelle animation (à composer) » en tête et par défaut, ligne de temps en haut du panneau, nom prêt à taper, rappel du déclencheur, guide sans piste) ; ligne de temps devant les déclencheurs repliés ; pioche d'éléments (aperçu, calques, fil d'Ariane, Échap) ; « Remplir avec » un préréglage sur une piste (`planFillTrackFromPreset`, testé) ; fil d'Ariane de la sélection dans tous les modes (`ui/Breadcrumb.tsx`, `lib/selection.ts`, testé) ; extrait du texte dans les libellés d'éléments sans nom (`nodeLabel`, testé) ; cadre pointillé de la piste active dans l'aperçu (`atelier:anim-targets`) ; badge « en double » (`duplicateQuickTriggers`, testé). Vérifié sur une copie des données : « Arrivée du héros » créée, nommée, composée à la pioche sur trois éléments remplis par préréglage et décalés, cadre de la piste active, Échap, badge en Design ; aucune erreur console ni serveur. Tests : 108 modèle, 53 rendu, 40 éditeur. Audit mis à jour (« Reprise »).
- 13 sept. 2026 (suite 4) — **Mode Animation, étape 7 (nettoyage, documents, audit) : cadrage terminé.** Nettoyage : anciennes aides d'apparition `planReveal`, `revealOf`, `planRemoveReveal` retirées (la palette « Apparition · … » passe par les choix rapides, les tests d'apparition aussi), `REVEAL_*` gardés pour la migration ; vocabulaire « run » remplacé par « déclencheur » (protocole `atelier:play`, inspecteur, aperçu) ; indication d'Effets qui renvoyait les apparitions à « Interactions » corrigée. Audit d'usage n°3 (`docs/audit-animation-2026-09.md`, trois scénarios : débutant en Écriture, arrivée du héros composée sur trois éléments, animation liée au défilement de la page) et corrections tirées de l'audit, en TDD quand c'est de la logique : retour au neutre écrit `none` sur une image-clé (sinon sautée) et `translateX` / `translateY` des préréglages réglables dans Effets (`lib/effects-css.ts`, 2 tests) ; temps exact d'une image-clé et départ d'une piste en ms ; animation vide sans piste ; avertissement quand un élément ajouté a ses propres animations ; libellés de bibliothèque avec élément et page (`animationLabel`, testé) ; Effets continus repliable. Documents : `fonctionnel.md` (section Animations restructurée, date, « pas encore là »), `document-model.md` § 8.4 (propriété absente d'une image-clé, effets continus, choix rapides, `pg`), cadrage (étape 7 cochée, réalisé), `CLAUDE.md` (structure). Restent à décider (audit, points 8 à 13) : contexte du panneau quand on choisit un élément à ajouter, remplir une piste avec un préréglage, sélection des conteneurs, homonymes, repère des cibles dans l'aperçu, doublons de choix rapides. Tests : 106 modèle, 53 rendu, 38 éditeur.
- 13 sept. 2026 (suite 3) — **Mode Animation, étape 6 (déclencheurs avancés, effets continus).** En TDD. Rendu : le script du site jouait le défilement et la souris avec `ease` sur toute la piste, le délai du déclencheur et un rang calculé sur un seul élément (décalages « depuis la fin » ou « du centre » faux) : aligné sur les autres déclencheurs ; un déclencheur de page reçoit `pg` et, au défilement, suit la progression de toute la page au lieu de la traversée de la racine (qui n'allait jamais de 0 à 1) (2 tests dans le DOM simulé). Modèle : `planAddPageTrigger`, `planUpdatePageTrigger`, `planRemovePageTriggerWithAnimation` (1 test). Éditeur : `openTrigger` et `validOpenTimeline` acceptent un déclencheur de page (hôte = racine), `triggerHosts` marque la racine (tests) ; panneau Animation restructuré (liste et réglages de déclencheurs communs à l'élément et à la page, plage de défilement en pourcentages expliqués, axe de la souris, section « Page · nom » avec chargement, défilement, souris), section « Effets continus » (`ContinuousEffects`, partagé avec Design : parallaxe, bandeau, compteur, carrousel automatique), rappel dans la ligne de temps de ce qui la parcourt. Vérifié : déclencheur de page au défilement créé depuis le panneau, titre du héros tourné à −8° en fin de ligne de temps (aperçu à −6,4° à mi-parcours) ; sur le site rendu, avec un vrai moteur, défilement à 0, 25, 50 et 100 % → 0, 250, 500 et 1 000 ms ; bandeau posé depuis Effets continus. Note : dans le navigateur intégré masqué, `requestAnimationFrame` ne tourne pas et le défilement ne se voit pas ; le script a été relancé avec un `requestAnimationFrame` immédiat pour la mesure. Tests : 106 modèle, 53 rendu, 35 éditeur. Prochaine étape : 7, nettoyage et documents.
- 13 sept. 2026 (suite 2) — **Mode Animation, étape 5 (préréglages).** En TDD. Modèle : `isPresetIntact` (un préréglage mis à l'échelle reste reconnu ; retouché, il est « personnalisé », et annuler le rend intact, sans effacer `preset`), `quickAnimation`, `planQuickAnimation` (poser, remplacer en gardant détail et délai, retirer avec son animation), `planQuickDetail` (d'un bloc, lettre par lettre 30 ms, enfants un à un 100 ms, sans toucher la ligne de temps), `planRemoveTriggerWithAnimation` (3 tests). Éditeur : `animation/QuickAnimations.tsx` partagé par Écriture (nouvelle section Animation de l'inspecteur, absente pour un rédacteur) et Design ; section Animations de Design réduite comme au cadrage § 4.3 (choix rapides, déclencheurs en lecture seule avec badge « personnalisée », Jouer, Ouvrir, retirer ; compteur) — les réglages des déclencheurs ne sont plus que dans le mode Animation ; palette « Animer « élément »… ». Vérifié dans l'éditeur (copie des données fichiers) : apparition Fondu lettre par lettre et survol Soulever posés en Design, retouche de l'opacité à 300 ms dans le mode Animation puis « Personnalisée (Fondu) » en Design et en Écriture, commande de palette qui ouvre la ligne de temps. Tests : 105 modèle, 51 rendu, 35 éditeur. Prochaine étape : 6, déclencheurs avancés (page, défilement, souris) et effets continus dans le panneau.
- 13 sept. 2026 (suite) — **Mode Animation, étape 4 (édition).** En TDD. Modèle : aides d'édition `keyframeStyleAt` (état d'un élément à un instant, sources `keyframe` exacte ou tenue et `rest`), `planUnsetKeyframeProp`, `planSetKeyframeEasing`, `planShiftKeyframes` (groupe, borné à 0, remplacement à l'arrivée, duplication) avec `shiftDelta`, `planRemoveKeyframes`, `planUpdateTrack`, `trackTargetFor`, `withTargetKind` (4 tests). Éditeur : `useKeyframeStyle` donne aux panneaux Design existants (Effets, Apparence, Dimensions, Espacement, Typographie) un mode image-clé sans les réécrire : se placer à un temps et régler une propriété pose l'image-clé ; pastilles ◆ image-clé (cliquer retire), ◇ tenue, ○ repos ; Effets ne garde que transformations et filtres. Ligne de temps sortie dans `animation/Timeline.tsx` : sélection d'images-clés (clic, ⇧-clic, portée de la piste), glisser en groupe, ⌥-glisser pour dupliquer, Suppr ; image-clé à la tête de lecture avec courbe du segment (`EasingField` : courbes usuelles, ressort avec raideur, amortissement, aperçu, stabilisation et « Caler le segment »), dupliquer, supprimer ; piste : ajouter l'élément sélectionné (`canAddTrack`), cible (élément, enfants, mots, lettres), décalage, retirer ; animation : durée, répétitions, aller-retour. `useDocument` expose `getSite()` (document courant sans attendre le rendu) : les aides d'animation réécrivent le tableau `animations`, et deux réglages dans le même instant (marges liées) s'écrasaient ; vérifié corrigé. Vérifié dans l'éditeur sur une copie des données fichiers : opacité réglée à 400 ms (aperçu exact à 0,5), glisser (+100 ms), ⌥-glisser, Suppr, ressort et calage du segment, ajout de la piste du titre en lettres décalées de 30 ms (aperçu lettre par lettre), marges liées, durée, boucle, aller-retour, retrait de piste ; aucune erreur serveur. Tests : 102 modèle, 51 rendu, 35 éditeur. À reprendre plus tard : l'indication d'Effets en mode Design renvoie encore aux « Interactions » pour les apparitions (étape 7). Prochaine étape : 5, préréglages en Écriture et en Design, palette.
- 13 sept. 2026 — **Mode Animation, étape 3 (lecture) : reprise et fin du travail en cours.** À la reprise, la copie de travail contenait l'étape commencée mais non commitée : panneau `AnimationModePanel` (déclencheurs, ajout, bibliothèque, lecteur, ligne de temps), aides `lib/timeline.ts` et leurs tests, messages `atelier:scrub` et l'aperçu à la tête de lecture, bouton « Ouvrir dans le mode Animation » de l'inspecteur. Le panneau n'était branché nulle part et le code ne compilait pas (4 erreurs TypeScript, 1 de lint). Ce travail est enregistré tel quel dans le commit `7b48979` pour garder la trace de ce qui était écrit ; la suite est dans les commits qui le suivent. Ajouts de la reprise, en TDD : (1) **rendu** : le script d'animation posait `ease` sur toute la piste en plus de la courbe du segment, contrairement au CSS ; aligné (courbe par image-clé, `ease` par défaut, effet linéaire), test dans le DOM simulé ; sans ce correctif la tête de lecture n'aurait pas montré l'état réel des animations jouées en CSS ; (2) **aperçu à la tête de lecture** extrait dans `lib/scrub.ts` (sans le délai du déclencheur ni ses répétitions, animations réutilisées, recréées si l'élément l'est), 4 tests ; (3) **branchement** dans `EditorShell` : onglet Animation (désactivé pour un rédacteur), panneau à la place de l'inspecteur, colonne élargie à 440 px avec une ligne de temps, animation ouverte rattachée à sa page et validée à chaque changement (`validOpenTimeline`, testée : elle se referme après annuler, retrait du déclencheur, suppression, changement de page), ouverture depuis la bibliothèque qui emmène sur la page de l'hôte, instant reposé au rechargement de l'aperçu, « Ouvrir dans le mode Animation » depuis l'inspecteur, commande « Mode Animation » de la palette ; (4) **calques** : éclair sur les éléments à déclencheur (`triggerHosts`, testé), point sur ceux qu'anime la ligne de temps ouverte ; (5) **aperçu en mode Animation** : sélection seulement, ni glisser ni édition de texte (cadrage § 4.1). Vérifié dans l'éditeur sur une copie des données fichiers de Maison Aurèle : tête de lecture (état à 400 ms), lecture jusqu'au bout, marques des calques, retour au repos en Design, ouverture depuis l'inspecteur, retrait du déclencheur puis ⌘Z, ouverture d'une animation de « La carte » depuis l'accueil, ajout d'un préréglage ; aucune erreur console ni serveur. Tests : 98 modèle, 51 rendu, 32 éditeur. Points relevés pour la suite : les animations issues de la migration 2 → 3 portent toutes le nom de leur préréglage (59 animations dans Maison Aurèle, dont beaucoup « Fondu en montant »), la bibliothèque est donc difficile à parcourir ; nommer d'après l'élément à l'étape 5 ou 7. Prochaine étape : 4, édition des images-clés par les panneaux Design.
- 11 sept. 2026 (durcissement sécurité) — Aperçu : HTML intégré et SVG rendus inertes dans l’éditeur, sortie de balise `style` neutralisée, accès des collaborateurs aligné sur les rôles. Export : téléchargement externe borné, redirections et destinations privées refusées. Formulaires : limiteur en fail-closed en production. CSP de l’éditeur complétée, build ajouté à la CI, Dependabot configuré, contrat documenté dans `docs/security.md`. Tests de régression ajoutés. Réglage GitHub restant à faire manuellement : protection de `main` avec PR et CI obligatoires.

- 11 sept. 2026 (suite 4) — **Cadrage du mode Animation** (`docs/cadrage-animation.md`) après essai : le premier système (runs par élément, formulaire dans l'inspecteur) est remplacé par des lignes de temps nommées (pistes, images-clés en ms, courbe par segment) lancées par des déclencheurs ; mode Animation à venir avec lecteur et ligne de temps à la place de l'inspecteur, préréglages en un geste en Écriture. Étapes 1 et 2 du cadrage livrées en TDD : modèle v3 (types, schéma, aides, migration 2 → 3 des documents et des instantanés, sites d'exemple), rendu sur le nouveau modèle (même moteur, tests adaptés : 98 modèle, 48 rendu dont le script en DOM simulé), inspecteur réduit aux déclencheurs et aux choix rapides, bibliothèque du thème. Prochaines étapes : mode Animation en lecture (3), édition (4), préréglages en Écriture (5), déclencheurs avancés (6), nettoyage (7).
- 11 sept. 2026 (suite 3) — Animations, étape 2 : **survol qui revient** (`reverseOnLeave`), **clic qui bascule** (`toggle`), **ressorts** (`easing: spring(raideur, amortissement)`, physique dans le modèle, `linear(…)` au rendu, durée calée sur la stabilisation, aperçu de la courbe dans l'inspecteur). TDD : 6 tests modèle, 2 rendu, 3 script (le `reverse()` de l'API Web Animations est simulé). Reste du plan : aperçu joué dans l'éditeur, séquences, scènes de défilement.
- 11 sept. 2026 (suite 2) — Animations, étape 1 du plan « système complet » : **cible** (l'élément, ses enfants ou les cartes d'une vue, un autre élément de la page, un sélecteur CSS), **découpage** d'un texte par mots ou par lettres, **décalage** par rang (depuis le début, la fin ou le centre). Contrat § 8.4, schéma sans nouvelle version (champs optionnels), rendu en CSS pur quand la cible est marquable (règle sur la cible, `calc()` avec `--at-i`/`--at-n`, `:has()` pour le survol d'un autre élément), script sinon ; test du script réel dans un DOM simulé (happy-dom, API Web Animations enregistrée). Travail mené en TDD : tests rouges d'abord (modèle 5, rendu 8, script 6, éditeur 2), puis code. Vérifié dans l'éditeur (cartes des points forts décalées de 120 ms, titre du héros lettre par lettre) et sur le site publié. Reste du plan : survol qui revient et clic qui bascule, ressorts, aperçu joué dans l'éditeur, séquences, scènes de défilement.
- 11 sept. 2026 (suite) — Système d'animation général (modèle § 8.4, version de schéma 2) : images-clés et déclencheurs (chargement, entrée dans l'écran, survol, clic, défilement) sur tout élément, seize préréglages, éditeur d'étapes, bibliothèque du site, « Jouer » dans l'aperçu ; rendu en CSS pur pour chargement et survol, script (API Web Animations) pour l'écran, le clic et le défilement, état de repos sans script ou avec « réduire les animations » ; bandeau à quatre sens avec pause au survol ; les apparitions n'écrivent plus dans le style et les anciens documents sont convertis à la lecture (migration 1 → 2, aussi sur l'instantané publié). Testé (modèle et rendu), vérifié dans l'éditeur et sur le site publié.
- 11 sept. 2026 — Lot 3 de l'audit UX : fenêtre Publier en deux onglets (Publier / Réglages du site) avec pied et raison du blocage, sous-domaine avec aperçu d'adresse, normalisation expliquée et disponibilité, refus de dépôt visible pendant le glisser, messages en français, choix oui / non en cases, position en liste, onglets et groupes radio au clavier (tabindex tournant, flèches), pile de dialogues pour Échap, styles partagés gérables dans Thème (préréglages par identifiant), import CSV avec pré-analyse et annulation, tableau des entrées avec filtre, tri et détail, tableau de bord avec modèles esquissés et zone d'accueil, glissement de valeur sur cinq propriétés de plus. Vérifié dans l'éditeur.
- 10 sept. 2026 (nuit) — Fin du lot 2 : bibliothèque et tableau en lecture seule de schéma pour un rédacteur, sélecteur ◇ sur tous les champs libres de style et validation en CSS brut, `Eyebrow` unique pour les intitulés, compteur « publiées pas encore en ligne » (l'API de publication renvoie les entrées de la version publiée). Vérifié dans l'éditeur.
- 10 sept. 2026 (soir, suite) — Lot 2 de l'audit UX : sections mémorisées par titre et ouverture par type, pastille d'origine lisible sans couleur et focalisable, région live permanente + bandeau d'erreur `alert` + annonce du contexte de réglage, rédacteur arrêté avant l'envoi et 403 non bloquant (remise à l'état enregistré), hors ligne avec compte à rebours et « Réessayer », conflit avec « Copier mes changements » et « Recharger » permanent, `beforeunload`, titres h2/h3 et interlettrage unique des intitulés, validation `CSS.supports` sur les champs de longueur, CSS brut avec jetons sans accolades, messages fermables avec « Annuler » après une suppression, statut « Brouillon / Publiée » en toutes lettres et messages reçus en lecture seule. Vérifié dans l'éditeur.
- 10 sept. 2026 (soir) — Lot 1 de l'audit UX : garde de saisie avant ⌘Z / ⌘K, touches destructives et de navigation limitées à l'espace de travail, arbre des calques focalisable (tabindex tournant, focus qui suit), « / » tapable en Écriture, focus sur Annuler dans les confirmations dangereuses, Échap sur le seul dialogue du dessus, quatre aides corrigées, `data-prop` sur le schéma de boîte, huit contrôles nommés, jetons `line-strong` / `dim` relevés et limites de champs en `line-strong`, dix `outline-none` inopérants retirés. Vérifié dans l'éditeur (⌘Z en champ, Suppr depuis un bouton, flèches dans l'arbre, « / » puis Échap ou Entrée).
- 10 sept. 2026 (après-midi) — Audit UX d'expert de l'éditeur (`docs/audit-ux-2026-09.md`) : parcours par tâches, heuristiques Nielsen / ISO 9241-110, architecture de l'inspecteur, accessibilité WCAG 2.2 et clavier, cohérence du système de design, performance perçue. Trois lectures de code et des mesures en direct ; dix corrections prioritaires et un plan en trois lots. Aucun code modifié.
- 10 sept. 2026 — Reprise de l'audit n°2 (`docs/audit-usage-restaurant.md`, § 18) : les onze points corrigés (mode par défaut, ancre, textes composés, placement des blocs, carte par base, champ de formulaire et clés uniques, FAQ dépliable, calques, apparitions en un geste et en cascade, style « Carte », titre `{titre}` des pages par entrée), plus l'exploration demandée : parallaxe, bandeau défilant, carrousel automatique, compteurs. Corrigés en route : CSS brut aveugle aux états, cible d'interaction non réécrite à la duplication, scripts du site joués deux fois dans l'aperçu (StrictMode) et écart d'hydratation de l'aperçu. « Maison Aurèle (audit) » remis au niveau du site codé depuis l'éditeur seul, version 1268 publiée ; le modèle codé aligné sur ce que l'éditeur produit (ancres natives, parallaxe, bandeau, carrousel auto, compteurs).
- 9 sept. 2026 — **Audit d'usage n°2 : Maison Aurèle reconstruite depuis l'éditeur** (`docs/audit-usage-restaurant.md`). Antoine a relevé que le site restaurant avait été écrit en code ; il a été refait de zéro, uniquement par l'interface, en une session : thème, bases importées, images, sept pages, composant à propriétés, apparitions, en-tête, publication (v227, sept adresses contrôlées). Verdict : faisable sans code sauf trois choses (mode sombre par défaut, ancres, textes composés « prix + € »), et huit points qui coincent, classés dans le document (placement des blocs ajoutés, carte par défaut des vues, Entrée dans les formulaires, questions fréquentes statiques, bloc Champ absent, noms dans les calques, apparitions à trois gestes, style Carte). Un bug corrigé en route : apparition invisible dans l'aperçu (`applyInstantStates`). Ces points forment la prochaine liste de travail avant tout ajout de fonctionnalité.

- 9 sept. 2026 — **Deuxième site : Maison Aurèle, restaurant** (revue point 15, D10). Construit de zéro dans le modèle : thème sombre par défaut avec mode clair (Fraunces + Manrope), neuf styles partagés, cinq composants (en-tête à variante transparent/plein, pied de page, témoignage, chiffre clé, bandeau de réservation à variante de ton, avec propriétés reliées), deux bases (plats : catégorie, étiquettes, prix, photo ; événements : date, programme en texte long, prix, places, page par entrée), sept pages (accueil, carte par catégories avec ancres et titres collants, événements, page événement, réservation avec formulaire à sept champs, la maison avec équipe et questions dépliables, 404), deux redirections, dix-huit entrées. Pour « un maximum d'animation », première tranche des **interactions déclaratives** (D31) : apparitions au défilement (sept effets, durée, délai, courbe, rejouer), masqué au chargement, afficher/masquer et changer de variante au clic ou au survol, défiler vers une section ; moteur (`data-ix` + script, inactif dans l'éditeur sauf l'état d'arrivée), panneau Interactions, tests. Gabarit « Exemple restaurant » à la création d'un site.

- 9 sept. 2026 — **Site de Marie en ligne.** Publié par Antoine depuis l'éditeur déployé ; formulaire de contact envoyé depuis le site public et reçu dans la base `frm_form_contact` (nom, email, message, page). Deux défauts repérés à cette occasion et corrigés (jetons de l'éditeur dans l'aperçu, base CSS des boutons). Lighthouse : à mesurer depuis Chrome (le quota de l'API PageSpeed était épuisé ce jour). Sortie de v0 : première case cochée, domaines à brancher, deuxième site à commencer (point 15).

- 9 sept. 2026 — **Bouton de formulaire sans fond** (repéré par Antoine dans l'éditeur). La base CSS du moteur (`.at-page button{border:0;background:none}`) avait plus de poids que le style du nœud (`.n-<id>`) : le fond et la bordure réglés dans Apparence n'apparaissaient ni dans l'éditeur ni en ligne. Toute la base des éléments passe en `:where()` (spécificité nulle) ; test ajouté.

- 9 sept. 2026 — **Bug de l'aperçu corrigé** (repéré par Antoine après la publication en ligne : « des éléments du thème ne passent pas »). C'était l'inverse : le site publié était juste, l'aperçu de l'éditeur faux. Depuis le temps B, l'éditeur copiait ses jetons `--color-ink`, `--color-accent`… en ligne sur `<html>` de l'iframe pour ses barres d'outils ; ce sont aussi les noms des jetons du thème du site, écrasés du coup (titres pâles, boutons bleus). Les jetons de l'éditeur sont maintenant posés sous `--atelier-ui-*`. Leçon notée pour `docs/document-model.md` : les variables CSS du site sont un espace de noms réservé.

- 9 sept. 2026 — **Éditeur déployé sur Vercel** (`https://atelier-editor.vercel.app`, `ATELIER_ALLOW_PATH_FALLBACK=1` le temps du domaine). Contrôlé depuis l'extérieur : redirection vers la connexion, site de Marie servi (version 900), plan du site, robots, 404, HSTS et nosniff. Correction dans la foulée : sans `ATELIER_SITES_DOMAIN`, `publicUrl` donnait `http://<sous-domaine>.localhost:3000` (canonique, plan du site, robots, fenêtre Publier) ; elle rend maintenant le chemin de repli derrière l'origine de la requête. Reste pour la sortie de v0 : connexion en ligne et republication par Antoine, envoi du formulaire, les deux domaines, Lighthouse, puis le deuxième site.

- 9 sept. 2026 — **Préparation de la mise en ligne.** `next build` de production vérifié en local (toutes les routes dynamiques), `next start` contrôlé avec les variables Supabase : redirection vers la connexion, page de connexion, site publié servi, 404, en-têtes. Constat : le dépôt n'a pas de distant Git, la table `site_members` manque sur Supabase, et le site de Marie y est publié (v900) sans sous-domaine enregistré ; repli par identifiant ajouté dans `findBySubdomain`. Étapes restantes, toutes côté Antoine : dépôt GitHub, bloc « partage », projet Vercel avec ses variables, les deux domaines, republication (`docs/mise-en-ligne.md`).

- 9 sept. 2026 — **Temps C, rôles et partage** (point 13). Modèle : `roles.ts` (propriétaire, éditeur, rédacteur ; `opAllowedForWriter` = la frontière Écriture/Design appliquée aux droits : contenu et structure des blocs oui, `style`, `hidden` et `site.set` non). Dépôts : membres par site (fichier : dans le document ; Supabase : table `site_members`, bloc « partage »), `listSites(user)` avec les sites partagés et le rôle, `isMember` pour la connexion, `publishEntries` (contenus seuls sous la version en ligne). Serveur : `siteRole`, `guardSite(id, min)` sur toutes les routes (rédacteur : lecture, entrées, fichiers, changements filtrés, contenus seuls ; éditeur : publication, retour arrière, export ; propriétaire : suppression, partage), route `/api/sites/:id/members`, invités acceptés au lien magique. Éditeur : rôle transmis à la coquille, rédacteur forcé en Écriture avec Design désactivé et Pages en lecture, fenêtre Publier avec « Publier les contenus seulement » et section Partage (inviter, changer le rôle, retirer), badge du rôle sur le tableau de bord, tableur au clavier (flèches, Entrée). Vérifié sur le serveur d'essai : invitation, rôle, retrait, adresse invalide refusée, contenus publiés sous la version 47. Tests : rôles (modèle), contrat des dépôts (membres, contenus seuls). À faire côté Supabase : bloc « partage » du schéma.

- 9 sept. 2026 — **Temps C, minimum professionnel** (point 12). Le site publié est désormais un gestionnaire de route qui renvoie le document HTML complet construit par `lib/html-document.ts`, la même fonction que l'export (un seul rendu, code head et fin de body insérés tels quels, plus de page React ni de layout `/s/`). Redirections (`matchRedirect` dans le modèle : exact ou `/dossier/*`, 301/302, `_redirects` à l'export), page « introuvable » conventionnelle à `/404` (créée d'un clic, servie en 404, non indexée, `404.html` à l'export), nom du site, code personnalisé et redirections dans la fenêtre Publier, destinataire par formulaire (`props.notifyTo`). Vérifié sur le serveur d'essai : 301 avec reste capturé, 404 avec la page du site et `noindex`, balise et script du head présents, plan du site sans `/404`, archive avec `404.html` et `_redirects`. Tests : redirections (modèle).

- 9 sept. 2026 — **Temps C, composants utilisateur** (point 11). Modèle : `components.ts` (créer depuis la sélection, détacher avec propriétés et emplacements résolus, supprimer si inutilisé, usages, variantes `axe:valeur`, chemin d'écriture et résolution du style sous variante), `applyOverrides` déplacé du moteur au modèle. Moteur : classes `v-<axe>-<valeur>` sur la racine de l'instance, `variantCss` (`.racine.v-… .nœud`), `RenderContext.extraClass`. Éditeur : « En faire un composant… » et « Détacher » (inspecteur et palette), section Composant sur la racine (nom, description, propriétés typées avec défaut et choix, axes de variantes, suppression), « Propriété du composant » pour relier texte/image/lien, section Composant de l'instance (variante par axe, valeurs par type), barre « Variante » qui fait écrire les panneaux Design dans `variantStyles`, clic dans une instance depuis l'aperçu qui ouvre le composant. Tests : 6 (modèle) + 1 (moteur).

- 9 sept. 2026 — **Temps C, export du code (première forme)** (`docs/export.md`). Nommage déduit dans le modèle (`naming.ts` : conteneur nommé → son nom, feuille → `<ancêtre>-<sorte>` numérotée, nom saisi prime, unicité dans le site), moteur capable de rendre avec ces classes (`RenderContext.classes`, `siteCss({ classes })`) tout en gardant `n-<id>` dans l'éditeur et le site publié, archive zip sans dépendance (`lib/zip.ts`), `GET /api/sites/:id/export` : une page HTML complète par adresse (10 pour le site d'exemple), feuille unique, médias renommés d'après la bibliothèque avec extension d'après leur signature, données JSON, document source, `_redirects`, README ; bouton « Télécharger le site (.zip) » dans Publier avec confirmation ; « Classe CSS » dans l'inspecteur. Vérifié : archive acceptée par `unzip -t`, aucun `n-` dans le CSS ni `data-node` dans le HTML, images en `.jpg`. Suite du temps C : composants utilisateur, minimum professionnel.

- 9 sept. 2026 — **Temps B de la revue, fait.** Modèle : `migrate.ts` (point de passage de toute lecture, refus d'une version plus récente), validation du document à la publication, identifiants descendants contrôlés à l'insertion et au remplacement, gestes d'écriture (`planSplit`, `planMergePrev`, `planSlashInsert`, `fitHeadings`) dans `text.ts` avec tests, types et schéma zod accordés (valeurs de style, types de nœuds, `appliesTo`, `name`), tests typés. Éditeur : protocole d'aperçu typé avec origine vérifiée, sérialisation extraite (`components/preview/serialize.ts`) et testée avec happy-dom (bug corrigé : retours à la ligne perdus au collage), suite de tests des dépôts exécutée contre les fichiers et contre Supabase quand les clés sont là, `setEntries` retiré, auteur réel dans le journal, `tsconfig` de l'app hérité de la base, workflow GitHub (typecheck, tests, lint). UI : boutons désactivés qui gardent leur explication, `ConfirmProvider`/`askConfirm` à la place des huit confirmations natives, lien saisi dans la barre de sélection (champ, pages du site en suggestion, Entrée/Échap), échec du remplacement de fichier signalé, export CSV annoncé seulement après réception, contrastes (`--color-dim`, toasts), `propLabel`, « Vue » / « Carte », « Tailles d'écran », « Tous les écrans », `mod()` pour ⌘/Ctrl, sélecteur d'image limité aux huit récentes avec la bibliothèque à portée partout, jetons de l'éditeur injectés dans l'aperçu, Écriture par défaut avec barre et rail allégés, balise, ratio et clé masqués en Écriture, `Toggle` pour les oui/non, focus piégé et restauré dans les dialogues, actions visibles au clavier.
- 9 sept. 2026 — **Temps A de la revue, fait.** Données : lecture par tranches des entrées et du journal (plus de troncature silencieuse à 1 000), compactage du journal et purge des instantanés (vingt gardés) à la publication, plafonds de corps avec message en français (2 Mo par modification, 4 Mo et 2 000 entrées par envoi, 64 Ko par message de formulaire, 12 Mo par image et 20 Mo par envoi), verrou complet sur les entrées en mode fichier. Sécurité : sites publiés servis seulement derrière leur sous-domaine en production (repli `/s/…` réservé au développement ou forcé par `ATELIER_ALLOW_PATH_FALLBACK`), refus de démarrer en production sans variables Supabase, sans connexion, sans liste d'adresses, ou avec `ATELIER_AUTH=off` / `ATELIER_STORE=file`, un site n'est accessible qu'à son propriétaire (script `assign-owner.mjs` pour les anciens), garde du propriétaire sur l'aperçu, sous-domaine validé et comparé par égalité, chemins de fichiers restreints et vérifiés après résolution, redirections `suite` et `Referer` bornées à l'origine, en-têtes de sécurité (`frame-ancestors 'self'` hors sites publiés, `nosniff`, `Referrer-Policy`, HSTS, `Permissions-Policy`), `postMessage` avec origine vérifiée des deux côtés, images : type déduit du contenu, SVG refusé, plafonds, `limitInputPixels`, envoi un par un, cookie de suite `secure`, formules neutralisées dans l'export CSV. Robustesse : un incident réseau remet le lot en file et réessaie avec délai croissant (rien n'est perdu), bandeau de conflit avec bouton Recharger, fenêtre Publier qui distingue enregistrement en cours et bloqué. Transport : garde du propriétaire sans charger le document, formulaire sans les entrées, limite d'envoi persistée en base (`rate_limits`, `rate_limit_hit`), CSS restreint à la page rendue (32 règles au lieu de toutes les pages), entrées envoyées à l'aperçu dans un message à part et filtrées aux bases de la page, feuille et ressources mémoïsées dans l'aperçu, un commit toutes les 50 ms pendant un glissement. Vérifié sur un serveur d'essai : en-têtes, repli et sous-domaine invalide, traversée refusée, referer étranger sans redirection, 3 Mo refusés avec message, 2 001 entrées refusées, 10 envois puis 429, éditeur fonctionnel (édition, entrées d'une vue, publication). À faire côté Supabase : bloc « limite de débit » du schéma, puis `assign-owner.mjs` pour le site de Marie.
- 8 sept. 2026 — Revue globale demandée par Antoine avant la mise en ligne. Verdict : fondations solides (frontières, opérations inversibles, moteur unique, index SQL), trois dettes réelles (transport en entier, sécurité de la mise en ligne, export absent). Plan A/B/C dans `docs/revue-globale-2026-09.md`.
- 8 sept. 2026 — Demande d'Antoine : régler une taille à la souris directement sur le champ du panneau. Fait pour tous les champs de longueur et de nombre de l'éditeur (`startDragValue`) : glisser sur le champ règle la valeur sans passer par l'édition, un clic sans mouvement met le curseur, Maj accélère, pas adaptés à l'unité, changements fusionnés en une seule étape d'historique ; l'ancien geste Alt + glisser sur l'unité disparaît. Vérifié sur un serveur d'essai sans connexion (`ATELIER_AUTH=off`) : glisser de 80 px sur la largeur minimale de l'image du héros donne 80px, une entrée d'historique, annulable. Retours d'Antoine : pas de centièmes (pas entiers par unité : 1 px, 1 %, un quart de rem, 0,1 sans unité) et le geste manquait sur les espacements (cases du schéma de boîte, ajoutées). Un glissement, quelle que soit sa durée, ne fait qu'une entrée d'historique (fenêtre de fusion étendue pendant le geste). L'espacement des lettres garde un pas de 0,01 em. Vérifié : remplissage haut 0 → 15 px en une entrée, espacement des lettres -0,01 → 0,29 em en une entrée, le tout annulé. Piste : quand le champ est vide, partir de la taille mesurée de l'élément plutôt que de zéro.
- 9 sept. 2026 — Comptes et tableau de bord : connexion par lien magique, multi-sites avec propriétaire, tableau de bord, site vierge. Vérifié sans connexion configurée : tableau de bord avec le site de Marie (en ligne, dix changements à publier), création de « Boulangerie Martin » vierge, éditeur et aperçu par site, publication, suppression depuis le tableau de bord. Corrigé au passage : le dépôt Supabase se passe de la colonne `owner` tant que le bloc « comptes » n'est pas exécuté ; les identifiants de site sont en minuscules et le sous-domaine dérivé est enregistré à la publication. Mise en service avec Antoine le 8 sept. : adresses Supabase corrigées (`.co`, pas `.com`), quota d'envoi intégré atteint puis Resend branché en SMTP (port 587 ; l'expéditeur d'essai `onboarding@resend.dev` n'écrit qu'à l'adresse du compte Resend, d'où la connexion avec `antoinaur@live.fr` en attendant un domaine vérifié) ; le lien magique ne vaut que dans le navigateur qui l'a demandé (message dédié) ; la liste des adresses autorisées est relue à chaque requête. Connexion réussie, tableau de bord ouvert.
- 9 sept. 2026 — Demandes d'Antoine : une explication claire de la vraie mise en ligne (`docs/mise-en-ligne.md`, à suivre ensemble) et un document fonctionnel et technique de l'état de l'outil (`docs/fonctionnel.md`, tenu à jour à chaque évolution, règle ajoutée dans `CLAUDE.md`).

- 6 sept. 2026 — M0 terminé. Supabase branché et vérifié. Remarques d'Antoine sur le mode Design intégrées dans M1 à M3. Trois décisions prises (identité, nom, hébergement). Prochaine étape : M1, système de design.
- 6 sept. 2026 (soir) — M1 posé : jetons sombres et denses (`globals.css`), primitives `src/ui`, coquille et inspecteur refaits, IBM Plex via next/font, icônes Lucide. Prochaine étape : M2, édition structurelle.
- 6 sept. 2026 (nuit) — M2 terminé : palette de blocs, glisser-déposer dans les calques et dans le canvas (planification pure et testée dans le modèle), largeur libre avec poignée et point de rupture actif, raccourcis clavier, renommage en place. Correctif : `null` vaut retrait sur un champ optionnel (les erreurs serveur affichent le détail). Prochaine étape : M3, panneaux Design.
- 7 sept. 2026 — M3 session 1 : résolution de style avec sources dans le modèle (testée), contrôles communs (pastille de source, champ de longueur, groupe exclusif, schéma de boîte), panneaux Disposition, Espacement, Dimensions, Responsive ; édition sur le point de rupture actif. Correctif : un retrait (`value` absente) est valide côté serveur. Session 2 : Typographie, Apparence, couleur et jetons, Effets.
- 7 sept. 2026 — M3 session 2 : sélecteur de couleur, Typographie, Apparence, Effets, panneaux Image, Lien, Balise et Collection. Session 3 à faire : états (survol…), styles partagés (appliquer, créer, modifier, détacher), thème (jetons), raccourcis et ⌘K, réglage des points de rupture, puis audit d'usage n°1.
- 8 sept. 2026 — M4 Écriture, première version : mode Écriture / Design réel dans la barre (mémorisé), édition riche dans le canvas, barres flottantes, menu /, Entrée et Retour arrière à la Notion, modèles de sections. Reste pour un M4 bis : listes à puces depuis le menu de bloc, sélection multiple, texte riche dans les champs de base de données. Exercice de construction d'une page en Écriture (8 sept.) : titre, Entrée, paragraphe, « / » → section Trois points forts, édition dans une carte avec Entrée qui reste dans la carte, déplacement d'une section par la poignée gauche : tout passe ; poignée et « + » déplacés à gauche du bloc comme dans Notion ; grille masquée en Écriture. Retours d'Antoine (8 sept.) : Entrée en début de bloc crée le bloc vide au-dessus et laisse le texte (comme Notion), Entrée dans un bouton ne crée rien ; la barre de bloc reste affichée jusqu'au survol d'un autre bloc (la poignée est atteignable) ; un texte relié à une base explique au clic qu'il se modifie dans la base ; Ctrl/⌘Z pendant la frappe annule la frappe puis, une fois épuisée, l'opération précédente. Suite : la barre de bloc ne saute plus sur le parent pendant le trajet vers la poignée et ne se cache plus en sortant du cadre ; poignée à l'intérieur du bloc près d'un bord ; même moteur d'édition riche en Design (double-clic) et en Écriture (clic), conformément à D06. Bug du bouton Écriture qui ne répondait pas au premier clic : divergence entre le pré-rendu serveur et l'état mémorisé dans le navigateur ; la coquille de l'éditeur n'est plus pré-rendue (client seulement). Glisser puis se raviser : relâcher sur sa propre place ne change rien (et n'entre pas dans l'historique), Échap annule le glissement ; les barres dans l'aperçu compensent l'échelle de réduction pour rester à taille réelle. Cible de dépôt sur l'espace vide d'un conteneur : la place la plus proche parmi ses enfants (bords de 8 px pour viser avant/après le conteneur), et la propre place de l'élément déplacé s'il en est le plus proche (rien ne bouge). Dans une rangée (flex en ligne, grille), avant/après se décide par la moitié gauche ou droite et le trait de dépôt est vertical ; un bouton ou un lien est une cible atomique (avant/après lui). Fantôme semi-transparent fidèle (styles calculés figés) qui suit le curseur. Un bouton ou un lien n'accepte que du texte, une image ou une icône (règle du modèle, testée).
- 8 sept. 2026 (suite) — Exercice n°2 en Écriture, page « Mariages » complète : neuf corrections (classement et mots-clés du menu /, bloc Citation, Échap conserve, sortie de liste, lien qui perdait son texte, vue de base atomique au dépôt, modèles en une colonne sur mobile, h1 unique, titres fluides). Restes à décider : le lien se saisit dans une boîte de dialogue native (à remplacer par un petit champ dans la barre de sélection) ; les vignettes de ressources sans nom affichent leur identifiant (as_p1…) ; le choix d'image se fait dans l'inspecteur à droite, pas dans le canvas (un clic sur une image vide pourrait ouvrir le choix). Note pour les tests automatisés : un rechargement à chaud (HMR) peut laisser l'aperçu figé sur un ancien état, ce n'est pas un défaut du produit (un rechargement propre le résout).
- 9 sept. 2026 — Import CSV et JSON des bases. Vérifié dans l'éditeur sur Projets : cinq colonnes reconnues (titre, catégorie, année, résumé, et « Lieu » créé comme nouveau champ), deux lignes importées en brouillon avec catégorie convertie depuis le libellé, année en nombre, cellule entre guillemets contenant un point-virgule conservée, adresse et ordre posés. Essais nettoyés. Retour d'Antoine : le téléchargement CSV marchait sans qu'on s'en rende compte ; il passe maintenant par une route serveur en pièce jointe, avec un message de confirmation, et un bouton « Copier » en secours.
- 9 sept. 2026 — Médias, second temps : usages, recherche, remplacement, nettoyage, texte alternatif sur la ressource (rendu testé). Vérifié dans l'éditeur : huit images, usages listés pour « Léa et Tom » (deux pages, deux entrées), « aller » ouvre la page et sélectionne l'image, texte alternatif enregistré, recherche, « Retirer » désactivé tant que l'image sert, choix par double-clic depuis le panneau Image. Piège : le compilateur React refuse une référence lue dans une commande mémorisée ; l'ouverture de la bibliothèque passe par une fonction de module enregistrée par le fournisseur.
- 9 sept. 2026 — M6 première version : publication par instantané, historique et retour arrière, site publié servi sur `/s/…` et par sous-domaine via `proxy.ts`, référencement automatique, plan du site et robots. Vérifié en mode fichier (second serveur, `ATELIER_STORE=file`, `NEXT_DIST_DIR=.next-file`) : jamais publié → publié à jour, page publiée avec titre, description, canonique, Open Graph, CSS et polices, liens préfixés en repli et racine derrière le sous-domaine (en-tête Host), page d'entrée en `og:type article`, script des formulaires présent sur Contact, dix adresses dans le plan du site, robots avec le plan, 404 sur une adresse inconnue, changement → publication v1 → retour à v0 visible aussitôt. Piège : `unstable_cache` gardait `null` et le profil `max` servait l'ancien état une fois de plus ; l'invalidation se fait avec `expire: 0`. Le dépôt en mémoire porte une version dans sa clé de cache (bumpée à chaque changement d'interface). À faire avec Antoine : exécuter le bloc « publication » du schéma Supabase, déployer sur Vercel.
- 9 sept. 2026 — M5 formulaires : réception, validation, anti-spam, enregistrement, notification, messages reçus dans Données, export CSV, panneaux Formulaire et Champ. Vérifié : envoi valide enregistré (nouveau), robot ignoré avec réponse normale, email invalide et champ manquant refusés avec message, envoi sans script redirigé, tableau des messages avec export. La notification n'est pas testée faute de clé Resend : à renseigner dans `.env.local` (`RESEND_API_KEY`, `FORM_NOTIFY_TO`). M5 est terminé, il reste le second temps des médias et l'interconnexion des bases avant M6.
- 9 sept. 2026 — **Pages par entrée liées aux bases** (proposition d'Antoine, comme Webflow) : créer une base crée sa page par entrée (case cochée par défaut, titre lié au champ titre, adresse `/<base>/{slug}`) ; supprimer la base supprime ses pages par entrée (annulable côté document) ; supprimer une page par entrée laisse la base sans page, avec avertissement ; l'onglet Pages sépare un groupe « Pages par entrée » avec l'icône base et le motif d'adresse, et la barre du haut porte le badge « page par entrée ». Une page par entrée sans entrée publiée se dessine quand même dans l'éditeur (`/preview/__template/<page>`, textes liés sur leur repli). Vérifié : création avec page, groupe dans l'onglet Pages, badge, rendu sans entrée, suppression de la base qui emporte sa page et ramène sur l'accueil.
- 9 sept. 2026 — Retours d'Antoine sur les bases : la section « Données » de l'inspecteur est maintenant toujours présente sur un texte, une image ou un lien, avec l'explication quand l'élément n'a pas de données à portée ; suppression d'une base depuis la vue tableur (confirmation qui annonce les entrées perdues, les vues à reconfigurer, les pages modèles qui redeviennent fixes ; entrées supprimées côté serveur, définition retirée du document, donc annulable côté document mais pas côté entrées). Observé : deux fenêtres ouvertes sur le même site donnent « Le site a été modifié ailleurs » dans la plus ancienne ; la synchronisation entre fenêtres viendra avec la collaboration (v2), en attendant recharger.
- 8 sept. 2026 (nuit, encore) — M5 modèle de page : `templateOf`, `dataSourceFor`, `entryPath` dans le modèle, panneau Données, sélecteur d'entrée, rôle de page. Vérifié dans l'éditeur : la page Projet s'ouvre sur une entrée, en changer recharge l'aperçu à l'adresse de l'entrée choisie, le résumé lié se relie au texte long puis revient, les réglages de la page montrent le rôle et le motif. Il reste dans M5 le formulaire de contact fonctionnel, puis le second temps des médias.
- 8 sept. 2026 (nuit, fin) — M5 vues de collection : CSS de disposition tiré de la vue, panneau complet (filtre, tris, limite, si vide, disposition, colonnes). Vérifié dans l'éditeur : filtre « Catégorie est Mariage » ramène deux cartes, colonnes 3 → 2 change la grille, défilement horizontal pose le flex avec accroche, tri par année décroissante réordonne. La page « Mariages » garde son filtre et son tri, qui lui vont bien. Suite de M5 : modèle de page (liaison des champs depuis le panneau, aperçu avec une entrée au choix), puis formulaire de contact.
- 8 sept. 2026 (nuit, suite) — M5 bases : onglet Données, vue tableur en dialogue, cellules par type, gestion des champs, API des entrées. Vérifié dans l'éditeur sur Supabase : modification d'un titre visible dans l'aperçu et enregistrée, nouvelle entrée en brouillon avec adresse automatique puis publiée et visible dans la vue de base, suppression, champ créé puis renommé (clé « lieu »), typé en choix avec options, déplacé, supprimé, galerie remplie depuis la bibliothèque puis vidée. Piège rencontré : le dépôt gardé en mémoire (`globalThis`) survivait au rechargement à chaud avec l'ancienne interface ; sa clé de cache porte maintenant une version. Restes pour plus tard : texte long en paragraphes seulement (le texte riche des champs viendra avec l'éditeur de texte riche, M4 bis), pas d'annulation sur les entrées, pas de tri ni de filtre dans le tableur, réordonner les entrées par glisser. Suite de M5 : vues de collection (disposition, filtre, tri, limite, colonnes par point de rupture) et modèle de page.
- 8 sept. 2026 (nuit) — M5 démarré par les médias : modèle (`Asset.name`, `Asset.variants`, spécification et schéma, testé), rendu `srcset` (testé), stockage de fichiers derrière une interface (`AssetStorage`, fichiers ou Supabase Storage), route d'import avec sharp, conversion HEIC côté navigateur, bibliothèque d'images et sélecteur renouvelés, clic sur une image vide dans l'aperçu. Vérifié de bout en bout sur Supabase : un JPEG de 2400 px donne quatre déclinaisons WebP en 3 s, servies publiquement. Limite connue : l'envoi passe par le serveur (4,5 Mo par requête sur Vercel), d'où la réduction dans le navigateur ; un envoi direct vers le stockage avec une adresse signée viendra si les originaux pleine taille deviennent nécessaires. Suite de M5 : édition des bases (vue tableur, entrées, champs image et galerie).
- 8 sept. 2026 (soir) — Séparateur invisible en Écriture : le `hr` gardait les bordures et la largeur automatique du navigateur (2 px de large dans une colonne alignée au début). Base CSS du moteur pour `hr`, orientation décidée par le conteneur, modèle « Séparateur » sans style local. Décision de positionnement du mode Écriture consignée ci-dessus. Piste notée : variantes de styles partagés sélectionnables en Écriture (M4 bis). Retour d'Antoine : Entrée sur un bloc sélectionné (section, image…) ne permettait pas d'ajouter un élément (la touche renommait le calque). En Écriture, Entrée sur un texte sélectionné reprend la frappe à la fin ; sur tout autre bloc, un paragraphe vide apparaît juste après lui, prêt à écrire (dedans, à la fin, si rien ne peut suivre le bloc). En Design, Entrée renomme toujours le calque. Décision d'Antoine (8 sept.) : Entrée sur une boîte sélectionnée met le paragraphe dedans, en dernier (la boîte s'ouvre) ; sur une feuille (image, bouton, vue, séparateur), juste après ; Entrée sur un paragraphe vide dernier de sa boîte sort de la boîte (règle des listes appliquée aux boîtes, planification `planExitBox` testée dans le modèle ; les régions de premier niveau d'une page ne se quittent pas). Le « + » de la barre de bloc reste « insérer après ».
- 7 sept. 2026 — Grille de mise en page (modèle `settings.layoutGrid`, cascade par point de rupture, testée) et calque dans l'éditeur.
- 7 sept. 2026 — M3 session 3 : états avec prévisualisation forcée, styles partagés complets, onglet Thème avec points de rupture, palette ⌘K. Reste : erreurs d'affichage à relever pendant l'audit d'usage n°1, qui est la prochaine étape.


## Journal — 11 septembre 2026 · durcissement UI

- [x] Renforcer le contraste des surfaces, textes secondaires et séparateurs.
- [x] Porter les contrôles principaux à des cibles plus confortables (28–32 px).
- [x] Rendre les onglets et états actifs perceptibles par fond, bordure et couleur.
- [x] Renforcer la sélection et les zones cliquables dans les calques.
- [x] Ajouter un mode concentration qui masque les deux panneaux.
- [x] Afficher un contexte de breakpoint persistant dans le canvas Design.
- [x] Aérer les sections de l’inspecteur et les dialogues.
- [x] Améliorer la hiérarchie et la densité du tableau de bord.
- [x] Ajouter une navigation persistante par catégories dans les réglages.
- [x] Ajouter la comparaison responsive côte à côte avec un aperçu mobile en lecture seule.

Les changements sont limités au système de design et à la composition de l’éditeur : aucun format de document, rendu de site ou contrat d’API n’est modifié.
